'use client'

// Placeholder — full implementation in Task 3.

type Props = {
  matchId: string
  rivalName: string
  initialScoreHome: number | null
  initialScoreAway: number | null
}

export function ResultEditor(_props: Props) {
  return null
}
