---
phase: 01-foundation-players-attendance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.mjs
  - src/app/globals.css
  - src/app/layout.tsx
  - public/manifest.json
  - public/icons/icon-192.png
  - public/icons/icon-512.png
  - .env.example
  - .gitignore
  - components.json
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/avatar.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/select.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/sonner.tsx
  - src/components/ui/form.tsx
  - src/lib/positions/constants.ts
  - src/lib/utils.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Project installs all Phase 1 runtime dependencies (supabase ssr, supabase-js, next-pwa, idb, react-hook-form, zod, @hookform/resolvers, sonner) at correct pinned versions"
    - "shadcn components in UI inventory are present under src/components/ui"
    - "globals.css uses dark-first palette per UI spec; club green is primary"
    - "Root layout uses Inter (lang=es), no Geist fonts, correct metadata for VRC Juveniles"
    - "next-pwa is wired in next.config.mjs and manifest.json + icons exist in /public"
    - "RUGBY_POSITIONS constant exists and exports 15 entries"
  artifacts:
    - path: "package.json"
      provides: "Pinned runtime dependencies for the entire app"
      contains: "@supabase/ssr"
    - path: "src/app/globals.css"
      provides: "Dark-first color tokens per UI spec"
      contains: "--background: oklch(0.12 0 0)"
    - path: "src/app/layout.tsx"
      provides: "Root layout in Spanish with Inter font and correct metadata"
      contains: "lang=\"es\""
    - path: "next.config.mjs"
      provides: "PWA wrapper config via next-pwa"
      contains: "withPWA"
    - path: "public/manifest.json"
      provides: "PWA manifest with VRC Juveniles identity"
      contains: "VRC Juveniles"
    - path: "src/lib/positions/constants.ts"
      provides: "Rugby positions 1-15 lookup table"
      contains: "RUGBY_POSITIONS"
  key_links:
    - from: "src/app/layout.tsx"
      to: "Inter font"
      via: "next/font/google"
      pattern: "Inter\\("
    - from: "next.config.mjs"
      to: "next-pwa"
      via: "withPWA wrapper"
      pattern: "withPWA"
---

<objective>
Install all Phase 1 runtime dependencies at pinned versions, add the shadcn component inventory required by the UI spec, replace the default light-mode shadcn tokens with the approved dark-first palette, fix the root layout (Inter font, Spanish lang, correct metadata), wire next-pwa with a VRC Juveniles manifest, and create the static rugby positions constants module.

Purpose: Wave 1 scaffold — every later wave depends on these dependencies, design tokens, and PWA wiring being in place.
Output: A buildable Next.js 14 PWA shell that matches the UI spec colors and typography but has no app screens yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-players-attendance/01-CONTEXT.md
@.planning/phases/01-foundation-players-attendance/01-RESEARCH.md
@.planning/phases/01-foundation-players-attendance/01-UI-SPEC.md
@CLAUDE.md
@package.json
@src/app/globals.css
@src/app/layout.tsx
@components.json

<interfaces>
<!-- Versions to install (from RESEARCH.md Standard Stack and CLAUDE.md) -->

Pinned exact versions (do NOT use ^):
- @supabase/ssr@0.10.2
- next-pwa@5.6.0

Caret versions:
- @supabase/supabase-js@^2.102.1
- idb@^8.0.3
- react-hook-form@^7.76.1
- zod@^4.4.3
- @hookform/resolvers (latest compatible)
- sonner (latest — also pulled by shadcn add sonner)

shadcn components to add (no third-party registries):
button card avatar badge input label select dialog popover separator skeleton sonner form
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 1 runtime dependencies and add shadcn components</name>
  <files>package.json, package-lock.json, components.json, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/avatar.tsx, src/components/ui/badge.tsx, src/components/ui/input.tsx, src/components/ui/label.tsx, src/components/ui/select.tsx, src/components/ui/dialog.tsx, src/components/ui/popover.tsx, src/components/ui/separator.tsx, src/components/ui/skeleton.tsx, src/components/ui/sonner.tsx, src/components/ui/form.tsx, src/lib/utils.ts</files>
  <read_first>
    - package.json (see currently installed deps; nothing app-specific installed yet)
    - components.json (confirm shadcn is initialized with style radix-nova)
    - CLAUDE.md §Technology Stack (pinned versions list)
    - .planning/phases/01-foundation-players-attendance/01-RESEARCH.md §Standard Stack
    - .planning/phases/01-foundation-players-attendance/01-UI-SPEC.md §Component Inventory
  </read_first>
  <action>
    Run the dependency install in two steps from the repo root (C:/dev/vrc-juveniles).

    Step A — install runtime + form deps with these exact specifiers:

    ```
    npm install @supabase/ssr@0.10.2 "@supabase/supabase-js@^2.102.1" next-pwa@5.6.0 "idb@^8.0.3" "react-hook-form@^7.76.1" "zod@^4.4.3" @hookform/resolvers
    ```

    Step B — add shadcn components (one command, all components). Use the project's pinned shadcn CLI (already at devDependencies / npx shadcn@latest is fine since shadcn is also in dependencies):

    ```
    npx shadcn@latest add button card avatar badge input label select dialog popover separator skeleton sonner form --yes --overwrite
    ```

    Notes:
    - If shadcn add prompts for cn helper path, accept the default (it will create/update src/lib/utils.ts with the standard cn function).
    - sonner shadcn block depends on the sonner npm package; if shadcn doesn't auto-install it, run `npm install sonner` afterwards.
    - The form shadcn component depends on react-hook-form + @hookform/resolvers + zod — all are installed in Step A so it should succeed without prompting.

    Do NOT install: xlsx, browser-image-compression, @dnd-kit/* (Phase 3), exceljs (Phase 2), react-beautiful-dnd (deprecated).

    After install, verify package.json contains @supabase/ssr at exactly "0.10.2" and next-pwa at exactly "5.6.0" (no caret). If the install added carets, edit package.json to remove them and re-run npm install to refresh the lockfile.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const fail=[];if(p.dependencies['@supabase/ssr']!=='0.10.2')fail.push('@supabase/ssr must be exactly 0.10.2, got '+p.dependencies['@supabase/ssr']);if(p.dependencies['next-pwa']!=='5.6.0')fail.push('next-pwa must be exactly 5.6.0, got '+p.dependencies['next-pwa']);for(const d of ['@supabase/supabase-js','idb','react-hook-form','zod','@hookform/resolvers','sonner']){if(!p.dependencies[d])fail.push('missing '+d);}if(fail.length){console.error(fail.join('\n'));process.exit(1);}console.log('deps OK');"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies includes `"@supabase/ssr": "0.10.2"` (exact, no caret)
    - package.json dependencies includes `"next-pwa": "5.6.0"` (exact, no caret)
    - package.json dependencies includes `@supabase/supabase-js`, `idb`, `react-hook-form`, `zod`, `@hookform/resolvers`, `sonner`
    - Files exist: src/components/ui/button.tsx, card.tsx, avatar.tsx, badge.tsx, input.tsx, label.tsx, select.tsx, dialog.tsx, popover.tsx, separator.tsx, skeleton.tsx, sonner.tsx, form.tsx
    - src/lib/utils.ts exists and exports `cn`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All Phase 1 dependencies installed at pinned versions; all 13 shadcn components present; TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Replace globals.css with dark-first palette and fix root layout (Inter, lang=es, metadata)</name>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <read_first>
    - src/app/globals.css (current light-mode :root)
    - src/app/layout.tsx (current Geist + lang=en + Create Next App metadata)
    - .planning/phases/01-foundation-players-attendance/01-UI-SPEC.md §Color Tokens (full table of dark-first values)
    - .planning/phases/01-foundation-players-attendance/01-UI-SPEC.md §Typography (Inter only)
  </read_first>
  <action>
    Replace the entire `:root` block in src/app/globals.css with the dark-first palette from UI-SPEC §Color Tokens. Use these exact values (keep the existing chart-1..chart-5 lines as-is; do not touch the .dark block):

    ```
    :root {
      --background: oklch(0.12 0 0);
      --foreground: oklch(0.95 0 0);
      --card: oklch(0.18 0 0);
      --card-foreground: oklch(0.95 0 0);
      --popover: oklch(0.18 0 0);
      --popover-foreground: oklch(0.95 0 0);
      --primary: oklch(0.40 0.12 142);
      --primary-foreground: oklch(0.97 0 0);
      --secondary: oklch(0.22 0 0);
      --secondary-foreground: oklch(0.90 0 0);
      --muted: oklch(0.22 0 0);
      --muted-foreground: oklch(0.60 0 0);
      --accent: oklch(0.40 0.12 142);
      --accent-foreground: oklch(0.97 0 0);
      --destructive: oklch(0.65 0.20 22);
      --border: oklch(1 0 0 / 10%);
      --input: oklch(1 0 0 / 12%);
      --ring: oklch(0.40 0.12 142);
      --chart-1: oklch(0.871 0.15 154.449);
      --chart-2: oklch(0.723 0.219 149.579);
      --chart-3: oklch(0.627 0.194 149.214);
      --chart-4: oklch(0.527 0.154 150.069);
      --chart-5: oklch(0.448 0.119 151.328);
      --radius: 0.625rem;
      --sidebar: oklch(0.16 0 0);
      --sidebar-foreground: oklch(0.85 0 0);
      --sidebar-primary: oklch(0.40 0.12 142);
      --sidebar-primary-foreground: oklch(0.97 0 0);
      --sidebar-accent: oklch(0.22 0 0);
      --sidebar-accent-foreground: oklch(0.90 0 0);
      --sidebar-border: oklch(1 0 0 / 8%);
      --sidebar-ring: oklch(0.40 0.12 142);
    }
    ```

    Rewrite src/app/layout.tsx so it:
    - Imports only `Inter` from `next/font/google` (delete the two `localFont` imports for Geist).
    - Does NOT import or reference GeistVF.woff or GeistMonoVF.woff (the font files in src/app/fonts can stay on disk for now; we'll delete them later if unused).
    - Sets `metadata` to `{ title: "VRC Juveniles", description: "Asistencia y gestión de jugadores juveniles - Virreyes Rugby Club", manifest: "/manifest.json", themeColor: "#2D5A1B" }`.
    - Uses `<html lang="es" className={inter.variable}>` and `<body className="font-sans antialiased">`.

    Final layout.tsx must look like:

    ```tsx
    import type { Metadata } from "next";
    import { Inter } from "next/font/google";
    import "./globals.css";

    const inter = Inter({ subsets: ["latin"], variable: "--font-sans" });

    export const metadata: Metadata = {
      title: "VRC Juveniles",
      description: "Asistencia y gestión de jugadores juveniles - Virreyes Rugby Club",
      manifest: "/manifest.json",
      themeColor: "#2D5A1B",
    };

    export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
      return (
        <html lang="es" className={inter.variable}>
          <body className="font-sans antialiased">{children}</body>
        </html>
      );
    }
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const css=fs.readFileSync('src/app/globals.css','utf8');const lay=fs.readFileSync('src/app/layout.tsx','utf8');const fail=[];if(!css.includes('--background: oklch(0.12 0 0)'))fail.push('globals.css missing dark --background');if(!css.includes('--primary: oklch(0.40 0.12 142)'))fail.push('globals.css missing club green --primary');if(!lay.includes('lang=\"es\"'))fail.push('layout.tsx must have lang=es');if(/GeistVF|GeistMono|localFont/.test(lay))fail.push('layout.tsx still references Geist');if(!lay.includes('VRC Juveniles'))fail.push('metadata title must be VRC Juveniles');if(!lay.includes('manifest.json'))fail.push('layout must declare manifest');if(fail.length){console.error(fail.join('\n'));process.exit(1);}console.log('shell OK');" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "--background: oklch(0.12 0 0)" src/app/globals.css` returns a match
    - `grep "--primary: oklch(0.40 0.12 142)" src/app/globals.css` returns a match
    - `grep 'lang="es"' src/app/layout.tsx` returns a match
    - `grep -L "Geist\|localFont" src/app/layout.tsx` (file does NOT contain Geist or localFont)
    - `grep "VRC Juveniles" src/app/layout.tsx` returns a match
    - `grep "manifest.json" src/app/layout.tsx` returns a match
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Dark-first palette active; root layout uses Inter only, declares Spanish, ships correct metadata + manifest link.</done>
</task>

<task type="auto">
  <name>Task 3: Wire next-pwa, create manifest + placeholder icons, env.example, and positions constants</name>
  <files>next.config.mjs, public/manifest.json, public/icons/icon-192.png, public/icons/icon-512.png, .env.example, .gitignore, src/lib/positions/constants.ts</files>
  <read_first>
    - next.config.mjs (current — may or may not exist; if it does, must wrap with withPWA)
    - .planning/phases/01-foundation-players-attendance/01-RESEARCH.md §PWA Configuration
    - .planning/phases/01-foundation-players-attendance/01-RESEARCH.md §Rugby Positions Constants
    - .gitignore (confirm .env.local is ignored — add it if missing)
  </read_first>
  <action>
    1) Create or rewrite `next.config.mjs` at the repo root with:

    ```js
    import nextPwa from "next-pwa";

    const withPWA = nextPwa({
      dest: "public",
      register: true,
      skipWaiting: true,
      disable: process.env.NODE_ENV === "development",
    });

    /** @type {import('next').NextConfig} */
    const nextConfig = {};

    export default withPWA(nextConfig);
    ```

    If next.config.js exists instead, delete it and replace with the .mjs above (Next 14 supports either; .mjs is cleaner for the ESM import).

    2) Create `public/manifest.json`:

    ```json
    {
      "name": "VRC Juveniles",
      "short_name": "VRC Juvs",
      "description": "Asistencia y gestión de jugadores juveniles - Virreyes Rugby Club",
      "start_url": "/",
      "display": "standalone",
      "orientation": "portrait",
      "background_color": "#1c1c1c",
      "theme_color": "#2D5A1B",
      "icons": [
        { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any maskable" },
        { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any maskable" }
      ]
    }
    ```

    3) Create placeholder PNG icons. Use Node to write a minimal solid-color PNG (club green #2D5A1B) at both sizes so next-pwa's manifest doesn't 404 in dev. Run this one-liner:

    ```
    node -e "const fs=require('fs');const zlib=require('zlib');function png(size){const w=size,h=size;const c=Buffer.from([0x2D,0x5A,0x1B,0xFF]);const row=Buffer.alloc(1+w*4,0);for(let x=0;x<w;x++){c.copy(row,1+x*4)}const raw=Buffer.concat(Array(h).fill(row));const data=zlib.deflateSync(raw);function chunk(type,d){const len=Buffer.alloc(4);len.writeUInt32BE(d.length);const t=Buffer.from(type);const crc=Buffer.alloc(4);const z=require('zlib');crc.writeUInt32BE(z.crc32?z.crc32(Buffer.concat([t,d])):require('buffer').Buffer.from(require('crypto').createHash('md5').update(Buffer.concat([t,d])).digest()).readUInt32BE(0));return Buffer.concat([len,t,d,crc])}const sig=Buffer.from([137,80,78,71,13,10,26,10]);const ihdr=Buffer.alloc(13);ihdr.writeUInt32BE(w,0);ihdr.writeUInt32BE(h,4);ihdr[8]=8;ihdr[9]=6;ihdr[10]=0;ihdr[11]=0;ihdr[12]=0;const buf=Buffer.concat([sig,chunk('IHDR',ihdr),chunk('IDAT',data),chunk('IEND',Buffer.alloc(0))]);return buf;}fs.mkdirSync('public/icons',{recursive:true});fs.writeFileSync('public/icons/icon-192.png',png(192));fs.writeFileSync('public/icons/icon-512.png',png(512));console.log('icons written');"
    ```

    If the inline crc32 is not available on the runtime, fall back to creating the icons with sharp or `npx @squoosh/cli` is overkill — simplest fallback: write a tiny placeholder using the `pngjs` approach below. If both approaches fail, create two solid-green PNGs manually using any image tool and commit them; the goal is non-zero PNG files at those paths.

    Fallback if the above one-liner fails: install pngjs as a dev dep and use it:

    ```
    npm install --save-dev pngjs
    node -e "const {PNG}=require('pngjs');const fs=require('fs');function make(size){const p=new PNG({width:size,height:size});for(let y=0;y<size;y++)for(let x=0;x<size;x++){const i=(size*y+x)<<2;p.data[i]=0x2D;p.data[i+1]=0x5A;p.data[i+2]=0x1B;p.data[i+3]=0xFF;}return PNG.sync.write(p);}fs.mkdirSync('public/icons',{recursive:true});fs.writeFileSync('public/icons/icon-192.png',make(192));fs.writeFileSync('public/icons/icon-512.png',make(512));console.log('icons written via pngjs');"
    ```

    4) Create `.env.example` at repo root:

    ```
    NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
    NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
    SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-server-only
    SUPABASE_ACCESS_TOKEN=your-personal-access-token-for-cli-push
    ```

    5) Ensure `.gitignore` contains `.env.local` and `public/sw.js*` (next-pwa generates sw.js and workbox-*.js at build time — these should not be committed). Add the following lines if absent:

    ```
    .env.local
    public/sw.js
    public/sw.js.map
    public/workbox-*.js
    public/workbox-*.js.map
    ```

    6) Create `src/lib/positions/constants.ts` exactly as specified in RESEARCH.md §Rugby Positions Constants (15 positions with number, name, group; export RUGBY_POSITIONS, PositionNumber, PositionGroup types).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const fail=[];const cfg=fs.readFileSync('next.config.mjs','utf8');if(!cfg.includes('withPWA'))fail.push('next.config.mjs missing withPWA');const m=JSON.parse(fs.readFileSync('public/manifest.json','utf8'));if(m.name!=='VRC Juveniles')fail.push('manifest name wrong');if(m.theme_color!=='#2D5A1B')fail.push('manifest theme_color must be #2D5A1B');if(!fs.existsSync('public/icons/icon-192.png'))fail.push('icon-192 missing');if(fs.statSync('public/icons/icon-192.png').size<100)fail.push('icon-192 too small');if(!fs.existsSync('public/icons/icon-512.png'))fail.push('icon-512 missing');if(fs.statSync('public/icons/icon-512.png').size<100)fail.push('icon-512 too small');const env=fs.readFileSync('.env.example','utf8');for(const k of ['NEXT_PUBLIC_SUPABASE_URL','NEXT_PUBLIC_SUPABASE_ANON_KEY','SUPABASE_SERVICE_ROLE_KEY','SUPABASE_ACCESS_TOKEN'])if(!env.includes(k))fail.push('.env.example missing '+k);const gi=fs.readFileSync('.gitignore','utf8');if(!gi.includes('.env.local'))fail.push('.gitignore missing .env.local');const pos=fs.readFileSync('src/lib/positions/constants.ts','utf8');if(!pos.includes('RUGBY_POSITIONS'))fail.push('positions constants missing export');const count=(pos.match(/number:\s*\d+/g)||[]).length;if(count!==15)fail.push('expected 15 positions, found '+count);if(fail.length){console.error(fail.join('\n'));process.exit(1);}console.log('pwa+constants OK');" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `next.config.mjs` exists and contains `withPWA`
    - `public/manifest.json` exists with `"name": "VRC Juveniles"` and `"theme_color": "#2D5A1B"`
    - `public/icons/icon-192.png` and `public/icons/icon-512.png` exist (size > 100 bytes each)
    - `.env.example` contains NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_ACCESS_TOKEN
    - `.gitignore` contains `.env.local`
    - `src/lib/positions/constants.ts` exports `RUGBY_POSITIONS` with 15 entries
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 (proves next-pwa wiring is valid and the project builds end-to-end)
  </acceptance_criteria>
  <done>PWA configured; manifest + icons in place; env template documented; rugby positions constant ready for player form consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Supply chain trust; pinned versions reduce drift |
| public/ assets → browser | All `public/` content is served as-is; nothing sensitive should live there |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information Disclosure) | .env.local secrets accidentally committed | mitigate | .gitignore includes `.env.local`; only `.env.example` with placeholders is tracked |
| T-01-02 | T (Tampering) | Service worker (public/sw.js generated by next-pwa) cached by browsers indefinitely | mitigate | `skipWaiting: true` in next-pwa config forces new SW to activate immediately on update; sw.js excluded from git so it cannot be tampered in source |
| T-01-03 | S (Spoofing) | Supply chain — drifting dep versions could pull in malicious updates | mitigate | @supabase/ssr and next-pwa pinned at exact versions; package-lock.json committed |
| T-01-04 | I | Service worker caches API responses including auth-scoped data | accept | Phase 1 next-pwa default config only precaches static assets; runtime caching of API is NOT configured — no auth data enters the SW cache |
</threat_model>

<verification>
1. `npm install` completes without errors.
2. `npx tsc --noEmit` exits 0.
3. `npm run build` exits 0 (proves PWA wiring + globals.css + layout.tsx all compose).
4. Manual smoke: `npm run dev`, open http://localhost:3000, page background is dark (~#1c1c1c) not white — confirms globals.css override is active.
</verification>

<success_criteria>
- All Phase 1 runtime dependencies installed at correct versions (pinned where specified).
- shadcn UI inventory present.
- Dark-first palette + Spanish + Inter shipped.
- next-pwa wired with manifest and placeholder icons.
- Rugby positions constants ready for Plan 04.
- `npm run build` succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-players-attendance/01-01-SUMMARY.md` summarizing dependency versions installed, files modified, and any deviations from the plan (e.g., pngjs fallback used for icons).
</output>
