---
phase: 02
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/app/(app)/fixture/actions.ts
  - src/components/fixture/MatchFormDialog.tsx
  - src/components/fixture/MatchFab.tsx
  - src/components/fixture/DeleteMatchDialog.tsx
  - src/components/fixture/FixtureListShell.tsx
autonomous: true
requirements: [FIX-03]
must_haves:
  truths:
    - "Coach puede tocar el FAB '+' en /fixture y abrir un dialog para agregar un partido"
    - "El dialog tiene campos: fecha, rival, local/visitante, hora, división (solo lectura para coach), subequipo (opcional)"
    - "Al guardar, el partido aparece en la lista del fixture inmediatamente (revalidatePath)"
    - "Admin puede editar el campo división del formulario; coach NO"
    - "Admin puede borrar un partido con confirmación; coach NO ve el botón borrar"
    - "Validación zod impide guardar campos requeridos vacíos con error legible en el form"
    - "Coach no puede crear/editar partidos de divisiones que no le corresponden (Server Action verifica autorización)"
  artifacts:
    - path: "src/app/(app)/fixture/actions.ts"
      provides: "Server Actions createMatch, updateMatch, deleteMatch"
      exports: ["createMatch", "updateMatch", "deleteMatch"]
    - path: "src/components/fixture/MatchFormDialog.tsx"
      provides: "Dialog reutilizable para crear y editar partido"
      contains: "MatchFormSchema"
    - path: "src/components/fixture/MatchFab.tsx"
      provides: "FAB flotante para abrir el dialog de crear"
      contains: "Agregar partido"
    - path: "src/components/fixture/DeleteMatchDialog.tsx"
      provides: "Dialog de confirmación de borrado admin-only"
      contains: "Eliminar partido"
  key_links:
    - from: "src/components/fixture/MatchFab.tsx"
      to: "src/components/fixture/MatchFormDialog.tsx"
      via: "open state via useState"
      pattern: "useState"
    - from: "src/components/fixture/MatchFormDialog.tsx"
      to: "src/app/(app)/fixture/actions.ts"
      via: "createMatch / updateMatch import"
      pattern: "from '@/app/\\(app\\)/fixture/actions'"
    - from: "src/app/(app)/fixture/actions.ts"
      to: "MatchFormSchema"
      via: "MatchFormSchema.parse"
      pattern: "MatchFormSchema\\.parse"
---

<objective>
Implementar FIX-03: crear, editar y eliminar partidos manualmente desde la UI. Entrega Server Actions con autorización (`requireAdminOrCoachForDivision`, `requireAdmin` para delete), un dialog reutilizable para create/edit, un FAB flotante en `/fixture`, y un dialog de confirmación de borrado admin-only.

Purpose: Sin este plan, el fixture solo se puede poblar via import CSV (Plan 05). Este plan es la vía manual paralela y la unica via para que el coach agregue partidos.

Output: Coach y admin pueden agregar partidos. Admin puede editarlos o borrarlos. Coach solo puede editar partidos de sus divisiones; el campo division del formulario esta bloqueado a su division activa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-fixture-management/02-CONTEXT.md
@.planning/phases/02-fixture-management/02-RESEARCH.md
@.planning/phases/02-fixture-management/02-UI-SPEC.md

<interfaces>
From src/lib/matches/schema.ts (Plan 01):
- MatchFormSchema (zod) con campos division_id, match_date, match_time, fecha_nro, rival, home_away (enum 'local'|'visitante'), venue, subequipo
- type MatchFormInput

From src/lib/supabase/database.types.ts (Plan 01):
- Database['public']['Tables']['matches']['Insert'] y ['Update']

From src/app/(app)/jugadores/actions.ts (patron a replicar exactamente):
- requireCoachForDivision(divisionId): patron de autorizacion via JOIN profiles + coach_divisions
- 'use server' directive, revalidatePath, eslint-disable any cast

From src/context/DivisionContext.tsx (Plan 02):
- useDivision() retorna { activeDivision, divisions, userRole }
- ALL_DIVISIONS_ID = '__all__'

From src/components/fixture/FixtureListShell.tsx (Plan 02):
- Client component que recibe allMatches e isAdmin
- Renderiza FixtureList con matches filtrados por division activa
- Este plan agrega el FAB al shell

shadcn components disponibles:
- Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter (de src/components/ui/dialog.tsx)
- Form, FormField, FormItem, FormControl, FormLabel, FormMessage (de src/components/ui/form.tsx)
- Input, Select, SelectTrigger, SelectContent, SelectItem, SelectValue
- Button (variants: default, secondary, ghost, destructive)
- Patron existente a inspeccionar: src/components/players/DeletePlayerDialog.tsx (si existe) y MatchForm en RESEARCH.md
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Server Actions createMatch / updateMatch / deleteMatch con autorizacion</name>
  <files>src/app/(app)/fixture/actions.ts</files>
  <read_first>
    - src/app/(app)/jugadores/actions.ts (patron Server Action + requireCoachForDivision + revalidatePath - copiar el estilo)
    - src/lib/matches/schema.ts (Plan 01 - MatchFormSchema y MatchFormInput)
    - src/lib/supabase/database.types.ts (Plan 01 - verificar Insert/Update types de matches)
    - src/lib/supabase/server.ts (createClient pattern)
    - .planning/phases/02-fixture-management/02-RESEARCH.md (Pattern 2)
  </read_first>
  <action>
Crear `src/app/(app)/fixture/actions.ts` con el contenido literal siguiente. No importar nada de jugadores/actions.ts: replicar el patron.

```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'
import { MatchFormSchema, type MatchFormInput } from '@/lib/matches/schema'
import type { Database } from '@/lib/supabase/database.types'

type ProfileRow = Database['public']['Tables']['profiles']['Row']
type MatchRow = Database['public']['Tables']['matches']['Row']

async function requireAdminOrCoachForDivision(
  divisionId: string
): Promise<{ userId: string; role: 'admin' | 'coach' | 'tutora' }> {
  const supabase = createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) throw new Error('No autorizado')

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: profileData } = await (supabase as any)
    .from('profiles').select('role').eq('id', user.id).single()

  const role = ((profileData as Pick<ProfileRow, 'role'> | null)?.role ?? 'coach') as 'admin' | 'coach' | 'tutora'
  if (role === 'admin') return { userId: user.id, role: 'admin' }

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: cdData } = await (supabase as any)
    .from('coach_divisions').select('division_id')
    .eq('coach_id', user.id).eq('division_id', divisionId).maybeSingle()

  if (!cdData) throw new Error('No tenes acceso a esta division')
  return { userId: user.id, role }
}

async function requireAdmin(): Promise<{ userId: string }> {
  const supabase = createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) throw new Error('No autorizado')

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data } = await (supabase as any)
    .from('profiles').select('role').eq('id', user.id).single()
  const role = (data as Pick<ProfileRow, 'role'> | null)?.role
  if (role !== 'admin') throw new Error('Solo admin puede realizar esta accion')
  return { userId: user.id }
}

async function requireAdminOrCoachForMatch(matchId: string): Promise<{ userId: string; role: 'admin' | 'coach' | 'tutora'; match: Pick<MatchRow, 'division_id' | 'manual'> }> {
  const supabase = createClient()
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: mData } = await (supabase as any)
    .from('matches').select('division_id, manual').eq('id', matchId).maybeSingle()
  if (!mData) throw new Error('Partido no encontrado')
  const match = mData as Pick<MatchRow, 'division_id' | 'manual'>
  const { userId, role } = await requireAdminOrCoachForDivision(match.division_id)
  return { userId, role, match }
}

export async function createMatch(input: MatchFormInput): Promise<{ matchId: string }> {
  const parsed = MatchFormSchema.parse(input)
  const { userId } = await requireAdminOrCoachForDivision(parsed.division_id)
  const supabase = createClient()

  const insertPayload: Database['public']['Tables']['matches']['Insert'] = {
    division_id: parsed.division_id,
    match_date: parsed.match_date,
    match_time: parsed.match_time || null,
    fecha_nro: parsed.fecha_nro ?? null,
    rival: parsed.rival,
    home_away: parsed.home_away,
    venue: parsed.venue || null,
    subequipo: parsed.subequipo || null,
    manual: true, // Manually created: preserved during URBA reimport (D-12)
    created_by: userId,
  }

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data, error } = await (supabase as any)
    .from('matches').insert(insertPayload).select('id').single()
  if (error || !data) throw new Error('No se pudo crear el partido. ' + (error?.message ?? ''))

  const insertedId = (data as { id: string }).id
  revalidatePath('/fixture')
  return { matchId: insertedId }
}

export async function updateMatch(matchId: string, input: MatchFormInput): Promise<void> {
  const parsed = MatchFormSchema.parse(input)
  const { role, match } = await requireAdminOrCoachForMatch(matchId)

  // Coach cannot move a match to a different division.
  if (role !== 'admin' && parsed.division_id !== match.division_id) {
    throw new Error('No podes cambiar la division del partido')
  }

  const updatePayload: Database['public']['Tables']['matches']['Update'] = {
    division_id: parsed.division_id,
    match_date: parsed.match_date,
    match_time: parsed.match_time || null,
    fecha_nro: parsed.fecha_nro ?? null,
    rival: parsed.rival,
    home_away: parsed.home_away,
    venue: parsed.venue || null,
    subequipo: parsed.subequipo || null,
  }

  const supabase = createClient()
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { error } = await (supabase as any)
    .from('matches').update(updatePayload).eq('id', matchId)
  if (error) throw new Error('No se pudo guardar el partido: ' + error.message)

  revalidatePath('/fixture')
  revalidatePath(`/fixture/${matchId}`)
}

export async function deleteMatch(matchId: string): Promise<void> {
  await requireAdmin()
  const supabase = createClient()
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { error } = await (supabase as any).from('matches').delete().eq('id', matchId)
  if (error) throw new Error('No se pudo eliminar el partido: ' + error.message)
  revalidatePath('/fixture')
}
```
  </action>
  <verify>
    <automated>grep -c "createMatch" "src/app/(app)/fixture/actions.ts" && grep -c "updateMatch" "src/app/(app)/fixture/actions.ts" && grep -c "deleteMatch" "src/app/(app)/fixture/actions.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/(app)/fixture/actions.ts` starts with `'use server'`
    - `grep -c "export async function createMatch" "src/app/(app)/fixture/actions.ts"` returns 1
    - `grep -c "export async function updateMatch" "src/app/(app)/fixture/actions.ts"` returns 1
    - `grep -c "export async function deleteMatch" "src/app/(app)/fixture/actions.ts"` returns 1
    - `grep -c "MatchFormSchema.parse" "src/app/(app)/fixture/actions.ts"` returns at least 2
    - `grep -c "requireAdmin" "src/app/(app)/fixture/actions.ts"` returns at least 2
    - `grep -c "manual: true" "src/app/(app)/fixture/actions.ts"` returns 1 (insert payload only)
    - `grep -c "revalidatePath('/fixture')" "src/app/(app)/fixture/actions.ts"` returns at least 3
    - `npx tsc --noEmit` runs without new errors
  </acceptance_criteria>
  <done>
    Server Actions createMatch, updateMatch, deleteMatch existen, autorizan correctamente, validan con zod, revalidan path /fixture y /fixture/[id].
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MatchFormDialog (crear/editar) + DeleteMatchDialog (admin-only)</name>
  <files>
    src/components/fixture/MatchFormDialog.tsx,
    src/components/fixture/DeleteMatchDialog.tsx
  </files>
  <read_first>
    - src/components/ui/dialog.tsx (verificar exports: Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter, DialogClose, DialogTrigger)
    - src/components/ui/form.tsx (verificar Form, FormField, FormItem, FormLabel, FormControl, FormMessage exports)
    - src/components/ui/input.tsx
    - src/components/ui/select.tsx (Select, SelectTrigger, SelectValue, SelectContent, SelectItem)
    - src/components/ui/button.tsx
    - src/lib/matches/schema.ts (Plan 01)
    - src/context/DivisionContext.tsx (Plan 02 - useDivision, divisions, userRole)
    - src/app/(app)/fixture/actions.ts (Task 1 - createMatch, updateMatch)
    - .planning/phases/02-fixture-management/02-UI-SPEC.md (Match Form section + Copywriting Contract)
    - Buscar en src/components/players/ si existe un dialog con form a usar como referencia (con react-hook-form si esta instalado, sino useState simple)
  </read_first>
  <action>
**Paso 1: chequear si `react-hook-form` y `@hookform/resolvers` estan en package.json.** Si lo estan, usar el patron de shadcn Form. Si NO estan, usar un useState simple con validacion via `MatchFormSchema.safeParse()`. Reportar cual via se eligio en el SUMMARY.

**Paso 2 - `src/components/fixture/MatchFormDialog.tsx`** (version sin react-hook-form, usa useState + safeParse - mas robusto sin asumir dependencias):

```typescript
'use client'

import { useState, useTransition } from 'react'
import { toast } from 'sonner'
import {
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter, DialogDescription,
} from '@/components/ui/dialog'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import {
  Select, SelectTrigger, SelectValue, SelectContent, SelectItem,
} from '@/components/ui/select'
import { Label } from '@/components/ui/label'
import { useDivision, isAllDivisions } from '@/context/DivisionContext'
import { MatchFormSchema, type MatchFormInput } from '@/lib/matches/schema'
import { createMatch, updateMatch } from '@/app/(app)/fixture/actions'

type ExistingMatch = {
  id: string
  division_id: string
  match_date: string
  match_time: string | null
  fecha_nro: number | null
  rival: string
  home_away: 'local' | 'visitante'
  venue: string | null
  subequipo: string | null
}

type Props = {
  open: boolean
  onOpenChange: (open: boolean) => void
  existing?: ExistingMatch
}

export function MatchFormDialog({ open, onOpenChange, existing }: Props) {
  const { activeDivision, divisions, userRole } = useDivision()
  const isAdmin = userRole === 'admin'
  const isEditing = !!existing

  // For coach: division is locked to activeDivision (or existing match's division).
  // For admin in "Todas las divisiones": let them pick from any.
  const defaultDivisionId =
    existing?.division_id ??
    (activeDivision && !isAllDivisions(activeDivision) ? activeDivision.id : divisions[0]?.id ?? '')

  const [divisionId, setDivisionId] = useState(defaultDivisionId)
  const [matchDate, setMatchDate] = useState(existing?.match_date ?? '')
  const [matchTime, setMatchTime] = useState(existing?.match_time ?? '')
  const [rival, setRival] = useState(existing?.rival ?? '')
  const [homeAway, setHomeAway] = useState<'local' | 'visitante'>(existing?.home_away ?? 'local')
  const [venue, setVenue] = useState(existing?.venue ?? '')
  const [subequipo, setSubequipo] = useState(existing?.subequipo ?? '')
  const [errors, setErrors] = useState<Record<string, string>>({})
  const [pending, startTransition] = useTransition()

  function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    setErrors({})

    const input: MatchFormInput = {
      division_id: divisionId,
      match_date: matchDate,
      match_time: matchTime || '',
      fecha_nro: null,
      rival: rival.trim(),
      home_away: homeAway,
      venue: venue.trim() || '',
      subequipo: subequipo.trim() || '',
    }

    const parsed = MatchFormSchema.safeParse(input)
    if (!parsed.success) {
      const fieldErrors: Record<string, string> = {}
      for (const issue of parsed.error.issues) {
        const key = issue.path.join('.')
        if (!fieldErrors[key]) fieldErrors[key] = issue.message
      }
      setErrors(fieldErrors)
      return
    }

    startTransition(async () => {
      try {
        if (isEditing && existing) {
          await updateMatch(existing.id, parsed.data)
          toast.success('Partido actualizado')
        } else {
          await createMatch(parsed.data)
          toast.success('Partido creado')
        }
        onOpenChange(false)
      } catch (err) {
        toast.error(err instanceof Error ? err.message : 'No se pudo guardar el partido')
      }
    })
  }

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="max-w-md">
        <DialogHeader>
          <DialogTitle>{isEditing ? 'Editar partido' : 'Agregar partido'}</DialogTitle>
          <DialogDescription>
            Completa los datos del partido. La division y la fecha son obligatorias.
          </DialogDescription>
        </DialogHeader>

        <form onSubmit={handleSubmit} className="space-y-4">
          <div className="space-y-2">
            <Label htmlFor="division">Division</Label>
            {isAdmin ? (
              <Select value={divisionId} onValueChange={setDivisionId}>
                <SelectTrigger id="division" className="h-11">
                  <SelectValue placeholder="Elegi una division" />
                </SelectTrigger>
                <SelectContent>
                  {divisions.map((d) => (
                    <SelectItem key={d.id} value={d.id}>{d.name}</SelectItem>
                  ))}
                </SelectContent>
              </Select>
            ) : (
              <Input
                id="division"
                value={divisions.find((d) => d.id === divisionId)?.name ?? ''}
                readOnly
                className="h-11 bg-muted"
              />
            )}
            {errors['division_id'] && <p className="text-sm text-destructive">{errors['division_id']}</p>}
          </div>

          <div className="grid grid-cols-2 gap-3">
            <div className="space-y-2">
              <Label htmlFor="match_date">Fecha</Label>
              <Input id="match_date" type="date" value={matchDate} onChange={(e) => setMatchDate(e.target.value)} className="h-11" />
              {errors['match_date'] && <p className="text-sm text-destructive">{errors['match_date']}</p>}
            </div>
            <div className="space-y-2">
              <Label htmlFor="match_time">Hora</Label>
              <Input id="match_time" type="time" value={matchTime} onChange={(e) => setMatchTime(e.target.value)} className="h-11" />
              {errors['match_time'] && <p className="text-sm text-destructive">{errors['match_time']}</p>}
            </div>
          </div>

          <div className="space-y-2">
            <Label htmlFor="rival">Rival</Label>
            <Input id="rival" placeholder="Nombre del rival" value={rival} onChange={(e) => setRival(e.target.value)} className="h-11" />
            {errors['rival'] && <p className="text-sm text-destructive">{errors['rival']}</p>}
          </div>

          <div className="grid grid-cols-2 gap-3">
            <div className="space-y-2">
              <Label htmlFor="home_away">Local/Visitante</Label>
              <Select value={homeAway} onValueChange={(v) => setHomeAway(v as 'local' | 'visitante')}>
                <SelectTrigger id="home_away" className="h-11">
                  <SelectValue />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="local">Local</SelectItem>
                  <SelectItem value="visitante">Visitante</SelectItem>
                </SelectContent>
              </Select>
            </div>
            <div className="space-y-2">
              <Label htmlFor="subequipo">Subequipo</Label>
              <Input id="subequipo" placeholder="A, B... (opcional)" value={subequipo} onChange={(e) => setSubequipo(e.target.value)} className="h-11" maxLength={4} />
            </div>
          </div>

          <div className="space-y-2">
            <Label htmlFor="venue">Sede</Label>
            <Input id="venue" placeholder="Cancha o club (opcional)" value={venue} onChange={(e) => setVenue(e.target.value)} className="h-11" />
          </div>

          <DialogFooter className="gap-2">
            <Button type="button" variant="ghost" onClick={() => onOpenChange(false)} disabled={pending}>
              Cancelar
            </Button>
            <Button type="submit" disabled={pending}>
              {pending ? 'Guardando...' : 'Guardar partido'}
            </Button>
          </DialogFooter>
        </form>
      </DialogContent>
    </Dialog>
  )
}
```

**Paso 3 - `src/components/fixture/DeleteMatchDialog.tsx`:**

```typescript
'use client'

import { useState, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import { toast } from 'sonner'
import {
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter, DialogDescription,
} from '@/components/ui/dialog'
import { Button } from '@/components/ui/button'
import { deleteMatch } from '@/app/(app)/fixture/actions'

type Props = {
  open: boolean
  onOpenChange: (open: boolean) => void
  matchId: string
  matchLabel: string  // e.g. "Banco Provincia - 12/04"
  redirectAfter?: string
}

export function DeleteMatchDialog({ open, onOpenChange, matchId, matchLabel, redirectAfter }: Props) {
  const router = useRouter()
  const [pending, startTransition] = useTransition()

  function handleDelete() {
    startTransition(async () => {
      try {
        await deleteMatch(matchId)
        toast.success('Partido eliminado')
        onOpenChange(false)
        if (redirectAfter) router.push(redirectAfter)
        else router.refresh()
      } catch (err) {
        toast.error(err instanceof Error ? err.message : 'No se pudo eliminar el partido')
      }
    })
  }

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="max-w-sm">
        <DialogHeader>
          <DialogTitle>Eliminar partido</DialogTitle>
          <DialogDescription>
            Eliminar este partido? Esta accion no se puede deshacer.
          </DialogDescription>
        </DialogHeader>
        <p className="text-sm text-muted-foreground">{matchLabel}</p>
        <DialogFooter className="gap-2">
          <Button type="button" variant="ghost" onClick={() => onOpenChange(false)} disabled={pending}>
            Cancelar
          </Button>
          <Button type="button" variant="destructive" onClick={handleDelete} disabled={pending}>
            {pending ? 'Eliminando...' : 'Eliminar'}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  )
}
```

Si `src/components/ui/label.tsx` no existe en el proyecto, agregarlo con `npx shadcn@latest add label`. Si tampoco existe `@/lib/utils` o `sonner`, verificar antes de continuar.
  </action>
  <verify>
    <automated>grep -c "MatchFormSchema.safeParse" src/components/fixture/MatchFormDialog.tsx && grep -c "createMatch\|updateMatch" src/components/fixture/MatchFormDialog.tsx && grep -c "deleteMatch" src/components/fixture/DeleteMatchDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/fixture/MatchFormDialog.tsx` starts with `'use client'`
    - `grep -c "MatchFormSchema.safeParse" src/components/fixture/MatchFormDialog.tsx` returns at least 1
    - `grep -c "from '@/app/(app)/fixture/actions'" src/components/fixture/MatchFormDialog.tsx` returns 1
    - `grep -c "Agregar partido\|Editar partido" src/components/fixture/MatchFormDialog.tsx` returns at least 2
    - `grep -c "userRole" src/components/fixture/MatchFormDialog.tsx` returns at least 1 (admin gate for division select)
    - `grep -c "readOnly" src/components/fixture/MatchFormDialog.tsx` returns at least 1 (coach sees readonly division)
    - File `src/components/fixture/DeleteMatchDialog.tsx` starts with `'use client'`
    - `grep -c "deleteMatch" src/components/fixture/DeleteMatchDialog.tsx` returns at least 1
    - `grep -c "Eliminar partido" src/components/fixture/DeleteMatchDialog.tsx` returns at least 1
    - `npx tsc --noEmit` runs without new errors
  </acceptance_criteria>
  <done>
    MatchFormDialog renderiza form completo, valida con zod, llama Server Actions, muestra toasts. DeleteMatchDialog confirma y borra (admin-only verifica en server).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: MatchFab + integrar FAB en FixtureListShell</name>
  <files>
    src/components/fixture/MatchFab.tsx,
    src/components/fixture/FixtureListShell.tsx
  </files>
  <read_first>
    - src/components/fixture/MatchFab.tsx (no existe aun)
    - src/components/fixture/FixtureListShell.tsx (Plan 02 - estado actual)
    - src/components/fixture/MatchFormDialog.tsx (Task 2 recien creado)
    - src/context/DivisionContext.tsx (useDivision)
    - .planning/phases/02-fixture-management/02-UI-SPEC.md (FAB section: 56px, bottom-right, var(--primary) bg, Plus icon, aria-label)
  </read_first>
  <action>
**Paso 1 - `src/components/fixture/MatchFab.tsx`:**

```typescript
'use client'

import { Plus } from 'lucide-react'

type Props = {
  onClick: () => void
}

export function MatchFab({ onClick }: Props) {
  return (
    <button
      type="button"
      onClick={onClick}
      aria-label="Agregar partido"
      className="fixed right-4 w-14 h-14 rounded-full bg-[color:var(--primary)] text-[color:var(--primary-foreground)] shadow-lg flex items-center justify-center active:scale-95 transition-transform focus-visible:ring-2 focus-visible:ring-[color:var(--ring)] focus-visible:outline-none"
      style={{ bottom: 'calc(80px + env(safe-area-inset-bottom))' }}
    >
      <Plus className="h-6 w-6" aria-hidden />
    </button>
  )
}
```

**Paso 2 - actualizar `src/components/fixture/FixtureListShell.tsx`** para montar el FAB y el dialog. Reemplazar el contenido por:

```typescript
'use client'

import { useMemo, useState } from 'react'
import { useDivision, isAllDivisions } from '@/context/DivisionContext'
import { FixtureList } from './FixtureList'
import { MatchFab } from './MatchFab'
import { MatchFormDialog } from './MatchFormDialog'

type MatchItem = {
  id: string
  division_id: string
  division_name?: string
  match_date: string
  rival: string
  home_away: 'local' | 'visitante'
  match_time: string | null
  fecha_nro: number | null
  subequipo: string | null
  score_home: number | null
  score_away: number | null
}

export function FixtureListShell({
  allMatches,
  isAdmin,
}: {
  allMatches: MatchItem[]
  isAdmin: boolean
}) {
  const { activeDivision } = useDivision()
  const [creating, setCreating] = useState(false)

  const filtered = useMemo(() => {
    if (!activeDivision || isAllDivisions(activeDivision)) {
      return isAdmin ? allMatches : []
    }
    return allMatches.filter((m) => m.division_id === activeDivision.id)
  }, [activeDivision, allMatches, isAdmin])

  // FAB only shown when a specific division is selected (cannot create without picking division
  // unless admin who can choose in the form). For admin in "Todas las divisiones", still show FAB
  // because the form has a division select.
  const showFab = !!activeDivision

  return (
    <>
      <FixtureList matches={filtered} />
      {showFab && <MatchFab onClick={() => setCreating(true)} />}
      <MatchFormDialog open={creating} onOpenChange={setCreating} />
    </>
  )
}
```

Notas:
- El FAB queda visible mientras haya una division activa (o "Todas" para admin). Para coaches sin divisiones asignadas, el AppLayout ya muestra PendingActivationScreen y no llega aqui.
- El MatchFormDialog se mantiene montado (open/closed via state) para no perder estado al cerrar.
  </action>
  <verify>
    <automated>grep -c "MatchFab" src/components/fixture/MatchFab.tsx && grep -c "MatchFab" src/components/fixture/FixtureListShell.tsx && grep -c "MatchFormDialog" src/components/fixture/FixtureListShell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/fixture/MatchFab.tsx` starts with `'use client'`
    - `grep -c "aria-label=\"Agregar partido\"" src/components/fixture/MatchFab.tsx` returns 1
    - `grep -c "calc(80px + env(safe-area-inset-bottom))" src/components/fixture/MatchFab.tsx` returns 1
    - `grep -c "var(--primary)" src/components/fixture/MatchFab.tsx` returns at least 1
    - `grep -c "w-14 h-14" src/components/fixture/MatchFab.tsx` returns 1 (56px size)
    - `grep -c "Plus" src/components/fixture/MatchFab.tsx` returns at least 1 (lucide icon)
    - `grep -c "import { MatchFab }" src/components/fixture/FixtureListShell.tsx` returns 1
    - `grep -c "import { MatchFormDialog }" src/components/fixture/FixtureListShell.tsx` returns 1
    - `grep -c "setCreating" src/components/fixture/FixtureListShell.tsx` returns at least 2
    - `npx tsc --noEmit` runs without new errors
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>
    FAB visible en /fixture, tocandolo abre el dialog, guardar crea el partido y aparece en la lista. Admin en "Todas las divisiones" puede elegir division en el form.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form -> Server Action | Coach input pasa validacion zod + autorizacion antes de tocar DB |
| Server Action -> matches table | RLS valida ademas de la autorizacion explicita |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Spoofing | createMatch | mitigate | requireAdminOrCoachForDivision verifica coach_divisions antes de insertar |
| T-02-13 | Tampering | updateMatch | mitigate | Si role != admin y division_id cambia: throw. RLS tambien aplica. |
| T-02-14 | Elevation of Privilege | deleteMatch | mitigate | requireAdmin throw para coach; RLS matches_delete tambien restringe a admin |
| T-02-15 | Tampering | MatchFormDialog division select | mitigate | Coach UI muestra Input readOnly; admin via Select; server valida igualmente (T-02-13) |
| T-02-16 | Input Validation | MatchFormDialog inputs | mitigate | MatchFormSchema.safeParse client-side + .parse server-side; doble validacion |
| T-02-17 | Cross-site request forgery | Server Actions | mitigate | Next.js Server Actions tienen CSRF protection nativa via origin check |
</threat_model>

<verification>
- Crear partido como coach: aparece en la lista de su division
- Editar partido: cambios reflejan en lista y detalle
- Coach intenta editar partido de otra division: error "No tenes acceso"
- Admin borra partido: confirmacion + lista actualiza
- Coach NO ve boton de borrar (lo controla quien renderice el DeleteMatchDialog)
- `npm run build` exitoso
</verification>

<success_criteria>
- FIX-03 implementado: alta y edicion manual de partido con campos requeridos
- Coach con division activa puede crear partido en su division
- Admin con "Todas las divisiones" puede crear partido eligiendo division
- Admin puede editar/borrar cualquier partido
- Coach no puede mover partido a otra division (server lo rechaza)
</success_criteria>

<output>
After completion, create `.planning/phases/02-fixture-management/02-03-SUMMARY.md`.
</output>
</content>
