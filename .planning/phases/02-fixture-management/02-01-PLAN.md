---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260530000000_matches.sql
  - src/lib/supabase/database.types.ts
  - src/lib/matches/schema.ts
  - src/lib/matches/utils.ts
  - src/lib/matches/csv-parser.ts
  - src/lib/queries/matches.ts
autonomous: false
requirements: [FIX-01, FIX-02, FIX-03, FIX-04, FIX-05, FIX-06, ADM-02, ADM-03]
must_haves:
  truths:
    - "Tablas matches y match_scoring_events existen en la DB con RLS activa"
    - "Los tipos de Supabase incluyen matches y match_scoring_events"
    - "El módulo lib/matches expone MatchFormSchema, ScoringEventSchema, parseFixtureCSV, getMatchResult, formatMatchDate"
    - "Las queries server-side listByDivision, listAllJuvenile, getById están disponibles para los componentes"
  artifacts:
    - path: "supabase/migrations/20260530000000_matches.sql"
      provides: "Schema matches + match_scoring_events + RLS policies"
      contains: "CREATE TABLE IF NOT EXISTS public.matches"
    - path: "src/lib/matches/schema.ts"
      provides: "MatchFormSchema y ScoringEventSchema (zod)"
      exports: ["MatchFormSchema", "MatchFormInput", "ScoringEventSchema", "ScoringEventInput"]
    - path: "src/lib/matches/utils.ts"
      provides: "getMatchResult, formatMatchDate"
      exports: ["getMatchResult", "formatMatchDate", "MatchResult"]
    - path: "src/lib/matches/csv-parser.ts"
      provides: "parseFixtureCSV"
      exports: ["parseFixtureCSV", "ParsedMatch", "CsvParseResult"]
    - path: "src/lib/queries/matches.ts"
      provides: "Server-side queries de matches"
      exports: ["listByDivision", "listAllJuvenile", "getById", "getWithEvents"]
  key_links:
    - from: "src/lib/queries/matches.ts"
      to: "src/lib/supabase/database.types.ts"
      via: "import type { Database }"
      pattern: "Database\\['public'\\]\\['Tables'\\]\\['matches'\\]"
    - from: "src/lib/matches/csv-parser.ts"
      to: "fixture-virreyes-2026.csv format"
      via: "parseo nativo split"
      pattern: "split\\('\\\\n'\\)"
---

<objective>
Crear los cimientos de datos y dominio para la fase 2: schema de DB (matches + match_scoring_events) con migration y RLS, regeneración de tipos Supabase, schemas de validación zod, utilidades de dominio (getMatchResult, formatMatchDate), parser de CSV, y queries server-side.

Purpose: Todos los plans 02-05 dependen de estos contratos. Sin este plan no hay tipos, no hay schema validado, no hay queries y no hay parser de CSV.

Output: Migration aplicada a Supabase, types regenerados, módulo `lib/matches` completo, queries server-side listas para consumir desde Server Components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fixture-management/02-CONTEXT.md
@.planning/phases/02-fixture-management/02-RESEARCH.md
@.planning/phases/02-fixture-management/02-UI-SPEC.md
@CLAUDE.md
@fixture-virreyes-2026.csv

<interfaces>
<!-- Tipos y patrones existentes que el executor debe replicar -->

From src/lib/queries/players.ts (patrón a replicar):
```typescript
import 'server-only'
import { createClient } from '@/lib/supabase/server'
import type { Database } from '@/lib/supabase/database.types'

export async function listByDivision(divisionId: string) {
  const supabase = createClient()
  const { data, error } = await supabase
    .from('players')
    .select('*, player_positions(...)')
    .eq('division_id', divisionId)
    .order(...)
  if (error) throw error
  return data ?? []
}
```

From src/lib/players/schema.ts (patrón zod a replicar):
```typescript
import { z } from 'zod'
export const PlayerFormSchema = z.object({...})
export type PlayerFormInput = z.infer<typeof PlayerFormSchema>
```

From supabase/migrations/20260528000000_training_sessions_attendance.sql (patrón RLS a replicar — JOIN explícito con profiles + coach_divisions, NO usar get_user_role() porque no está garantizado que exista — ver Assumption A2 en RESEARCH.md):
```sql
CREATE POLICY "coaches_read_X" ON public.X
  FOR SELECT USING (
    EXISTS (SELECT 1 FROM public.profiles WHERE id = auth.uid() AND role = 'admin')
    OR EXISTS (
      SELECT 1 FROM public.coach_divisions
      WHERE coach_id = auth.uid() AND division_id = X.division_id
    )
  );
```

From fixture-virreyes-2026.csv (formato exacto):
- Header: `division,fecha_nro,fecha,equipo,local_visitante,rival,hora`
- Ejemplo fila: `M19,1,2026-04-11,A,Local,Banco Provincia,16:00`
- Divisiones válidas: M15, M16, M17, M19
- local_visitante válido: Local, Visitante
- equipo puede ser vacío para divisiones sin subequipo
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear migration matches + match_scoring_events con RLS</name>
  <files>supabase/migrations/20260530000000_matches.sql</files>
  <read_first>
    - supabase/migrations/20260528000000_training_sessions_attendance.sql (patrón RLS exacto a replicar — JOIN con profiles y coach_divisions, NO get_user_role())
    - supabase/migrations/20260527000000_player_positions.sql (patrón de migration con BEGIN/COMMIT)
    - src/lib/supabase/database.types.ts (verificar que existen tablas divisions, profiles, players, coach_divisions con los IDs correctos)
    - .planning/phases/02-fixture-management/02-RESEARCH.md (§ Database Schema — bloque SQL completo a copiar)
  </read_first>
  <behavior>
    - Migration crea tabla `matches` con columnas exactas: id (uuid PK), division_id (uuid FK divisions ON DELETE CASCADE NOT NULL), match_date (date NOT NULL), match_time (time NULL), fecha_nro (integer NULL), rival (text NOT NULL), home_away (text NOT NULL CHECK IN 'local'/'visitante'), venue (text NULL), subequipo (text NULL), score_home (integer NULL), score_away (integer NULL), manual (boolean NOT NULL DEFAULT FALSE), created_by (uuid FK profiles), created_at (timestamptz NOT NULL DEFAULT now()), updated_at (timestamptz NOT NULL DEFAULT now()).
    - Migration crea tabla `match_scoring_events` con: id (uuid PK), match_id (uuid FK matches ON DELETE CASCADE NOT NULL), team (text NOT NULL CHECK IN 'home'/'away'), event_type (text NOT NULL CHECK IN 'try','conversion','penalty','drop','yellow_card','red_card'), player_id (uuid FK players ON DELETE SET NULL NULL), rival_scorer (text NULL), created_at (timestamptz NOT NULL DEFAULT now()).
    - Constraint: score_home y score_away deben ser ambos NULL o ambos NOT NULL (CHECK constraint).
    - RLS activa en ambas tablas.
    - 4 policies para `matches`: SELECT (admin OR coach_divisions match), INSERT WITH CHECK (admin OR coach_divisions match), UPDATE USING (admin OR coach_divisions match), DELETE USING (admin only).
    - 4 policies para `match_scoring_events`: SELECT/INSERT/UPDATE/DELETE via JOIN con matches y aplicar misma regla de división del partido padre. DELETE admin-only.
    - Indexes: idx_matches_division_date ON matches(division_id, match_date), idx_matches_manual ON matches(manual), idx_scoring_match ON match_scoring_events(match_id).
    - Trigger BEFORE UPDATE en matches que actualice updated_at a now().
  </behavior>
  <action>
Crear el archivo `supabase/migrations/20260530000000_matches.sql` con este contenido literal (Spanish identifiers preserved, RLS pattern copiado de training_sessions, NO usar get_user_role() — usar JOIN explícito con profiles según Assumption A2 de RESEARCH.md):

```sql
-- Phase 02 Plan 01: matches + match_scoring_events
-- Adds the fixture (calendar of matches) and rugby scoring events tables.
-- RLS replicates the pattern from training_sessions: explicit JOIN with profiles + coach_divisions.

-- ─── matches ────────────────────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS public.matches (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  division_id   uuid NOT NULL REFERENCES public.divisions(id) ON DELETE CASCADE,
  match_date    date NOT NULL,
  match_time    time,
  fecha_nro     integer,
  rival         text NOT NULL,
  home_away     text NOT NULL CHECK (home_away IN ('local', 'visitante')),
  venue         text,
  subequipo     text,
  score_home    integer,
  score_away    integer,
  manual        boolean NOT NULL DEFAULT false,
  created_by    uuid REFERENCES public.profiles(id),
  created_at    timestamptz NOT NULL DEFAULT now(),
  updated_at    timestamptz NOT NULL DEFAULT now(),
  CONSTRAINT matches_score_consistency CHECK (
    (score_home IS NULL AND score_away IS NULL)
    OR (score_home IS NOT NULL AND score_away IS NOT NULL)
  )
);

CREATE INDEX IF NOT EXISTS idx_matches_division_date ON public.matches(division_id, match_date);
CREATE INDEX IF NOT EXISTS idx_matches_manual ON public.matches(manual);

-- updated_at trigger
CREATE OR REPLACE FUNCTION public.matches_set_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = now();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS trg_matches_updated_at ON public.matches;
CREATE TRIGGER trg_matches_updated_at
  BEFORE UPDATE ON public.matches
  FOR EACH ROW
  EXECUTE FUNCTION public.matches_set_updated_at();

ALTER TABLE public.matches ENABLE ROW LEVEL SECURITY;

CREATE POLICY "matches_read" ON public.matches
  FOR SELECT USING (
    EXISTS (SELECT 1 FROM public.profiles WHERE id = auth.uid() AND role = 'admin')
    OR EXISTS (
      SELECT 1 FROM public.coach_divisions
      WHERE coach_id = auth.uid() AND division_id = matches.division_id
    )
  );

CREATE POLICY "matches_insert" ON public.matches
  FOR INSERT WITH CHECK (
    EXISTS (SELECT 1 FROM public.profiles WHERE id = auth.uid() AND role = 'admin')
    OR EXISTS (
      SELECT 1 FROM public.coach_divisions
      WHERE coach_id = auth.uid() AND division_id = matches.division_id
    )
  );

CREATE POLICY "matches_update" ON public.matches
  FOR UPDATE USING (
    EXISTS (SELECT 1 FROM public.profiles WHERE id = auth.uid() AND role = 'admin')
    OR EXISTS (
      SELECT 1 FROM public.coach_divisions
      WHERE coach_id = auth.uid() AND division_id = matches.division_id
    )
  );

CREATE POLICY "matches_delete" ON public.matches
  FOR DELETE USING (
    EXISTS (SELECT 1 FROM public.profiles WHERE id = auth.uid() AND role = 'admin')
  );

-- ─── match_scoring_events ──────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS public.match_scoring_events (
  id           uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  match_id     uuid NOT NULL REFERENCES public.matches(id) ON DELETE CASCADE,
  team         text NOT NULL CHECK (team IN ('home', 'away')),
  event_type   text NOT NULL CHECK (event_type IN ('try', 'conversion', 'penalty', 'drop', 'yellow_card', 'red_card')),
  player_id    uuid REFERENCES public.players(id) ON DELETE SET NULL,
  rival_scorer text,
  created_at   timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_scoring_match ON public.match_scoring_events(match_id);

ALTER TABLE public.match_scoring_events ENABLE ROW LEVEL SECURITY;

CREATE POLICY "scoring_read" ON public.match_scoring_events
  FOR SELECT USING (
    EXISTS (
      SELECT 1 FROM public.matches m
      JOIN public.profiles p ON p.id = auth.uid()
      WHERE m.id = match_scoring_events.match_id
        AND (p.role = 'admin' OR EXISTS (
          SELECT 1 FROM public.coach_divisions cd
          WHERE cd.coach_id = auth.uid() AND cd.division_id = m.division_id
        ))
    )
  );

CREATE POLICY "scoring_insert" ON public.match_scoring_events
  FOR INSERT WITH CHECK (
    EXISTS (
      SELECT 1 FROM public.matches m
      JOIN public.profiles p ON p.id = auth.uid()
      WHERE m.id = match_scoring_events.match_id
        AND (p.role = 'admin' OR EXISTS (
          SELECT 1 FROM public.coach_divisions cd
          WHERE cd.coach_id = auth.uid() AND cd.division_id = m.division_id
        ))
    )
  );

CREATE POLICY "scoring_update" ON public.match_scoring_events
  FOR UPDATE USING (
    EXISTS (
      SELECT 1 FROM public.matches m
      JOIN public.profiles p ON p.id = auth.uid()
      WHERE m.id = match_scoring_events.match_id
        AND (p.role = 'admin' OR EXISTS (
          SELECT 1 FROM public.coach_divisions cd
          WHERE cd.coach_id = auth.uid() AND cd.division_id = m.division_id
        ))
    )
  );

CREATE POLICY "scoring_delete" ON public.match_scoring_events
  FOR DELETE USING (
    EXISTS (
      SELECT 1 FROM public.matches m
      JOIN public.profiles p ON p.id = auth.uid()
      WHERE m.id = match_scoring_events.match_id
        AND (p.role = 'admin' OR EXISTS (
          SELECT 1 FROM public.coach_divisions cd
          WHERE cd.coach_id = auth.uid() AND cd.division_id = m.division_id
        ))
    )
  );
```

NO modificar el database.types.ts manualmente — eso lo hace la siguiente task con `supabase gen types`.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS public.matches" supabase/migrations/20260530000000_matches.sql && grep -c "match_scoring_events" supabase/migrations/20260530000000_matches.sql && grep -c "matches_score_consistency" supabase/migrations/20260530000000_matches.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260530000000_matches.sql` exists
    - `grep -c "CREATE TABLE IF NOT EXISTS public.matches" supabase/migrations/20260530000000_matches.sql` returns 1
    - `grep -c "CREATE TABLE IF NOT EXISTS public.match_scoring_events" supabase/migrations/20260530000000_matches.sql` returns 1
    - `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260530000000_matches.sql` returns 2
    - `grep -c "matches_score_consistency CHECK" supabase/migrations/20260530000000_matches.sql` returns 1
    - `grep -c "home_away IN ('local', 'visitante')" supabase/migrations/20260530000000_matches.sql` returns 1
    - `grep -c "event_type IN ('try', 'conversion', 'penalty', 'drop', 'yellow_card', 'red_card')" supabase/migrations/20260530000000_matches.sql` returns 1
    - `grep -c "ON DELETE CASCADE" supabase/migrations/20260530000000_matches.sql` returns at least 2 (division_id, match_id)
    - `grep -c "ON DELETE SET NULL" supabase/migrations/20260530000000_matches.sql` returns 1 (player_id)
    - `grep -c "trg_matches_updated_at" supabase/migrations/20260530000000_matches.sql` returns at least 1
    - El archivo NO contiene la string "get_user_role()" (porque la función no está garantizada en el schema compartido — usar JOIN explícito)
  </acceptance_criteria>
  <done>
    Migration file existe con las dos tablas, RLS, policies, constraint de consistencia score, indexes y trigger updated_at.
  </done>
</task>

<task type="auto" tdd="false" gate="blocking">
  <name>Task 2: [BLOCKING] Push migration y regenerar database.types.ts</name>
  <files>src/lib/supabase/database.types.ts</files>
  <read_first>
    - supabase/migrations/20260530000000_matches.sql (la migration recién creada)
    - src/lib/supabase/database.types.ts (estado actual antes del regen)
    - package.json (verificar script `db:types` o el comando para regenerar)
  </read_first>
  <action>
1. Ejecutar el push de migrations a Supabase:

```bash
supabase db push
```

Si el comando requiere SUPABASE_ACCESS_TOKEN no interactivo, usar:
```bash
SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN supabase db push --linked
```

Si el push falla por TTY (Windows PowerShell), correr en modo no-interactivo con `--include-all` o pedirle al usuario que ejecute el push manualmente y continúe.

2. Regenerar tipos TypeScript ejecutando el script existente del proyecto. Primero verificar en `package.json` qué script existe (típicamente `npm run db:types` o `supabase gen types typescript --linked > src/lib/supabase/database.types.ts`). Si no hay script, ejecutar directamente:

```bash
supabase gen types typescript --linked > src/lib/supabase/database.types.ts
```

3. Verificar que `src/lib/supabase/database.types.ts` ahora contiene las definiciones de tipo para `matches` y `match_scoring_events` con todas las columnas (id, division_id, match_date, match_time, fecha_nro, rival, home_away, venue, subequipo, score_home, score_away, manual, created_by, created_at, updated_at; y para scoring events: id, match_id, team, event_type, player_id, rival_scorer, created_at).

Si por alguna razón el push no se puede completar de forma autónoma (e.g. requiere login interactivo), marcar este task como bloqueado y pedir al usuario que ejecute manualmente `supabase db push` y `supabase gen types typescript --linked > src/lib/supabase/database.types.ts` y confirme antes de continuar. Este task NO se puede saltar — la verificación posterior (build, queries) falla sin tipos generados.
  </action>
  <verify>
    <automated>grep -c "matches:" src/lib/supabase/database.types.ts && grep -c "match_scoring_events:" src/lib/supabase/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "matches:" src/lib/supabase/database.types.ts` returns at least 1
    - `grep -c "match_scoring_events:" src/lib/supabase/database.types.ts` returns at least 1
    - `grep -c "home_away" src/lib/supabase/database.types.ts` returns at least 1
    - `grep -c "score_home" src/lib/supabase/database.types.ts` returns at least 1
    - `grep -c "event_type" src/lib/supabase/database.types.ts` returns at least 1
    - `grep -c "fecha_nro" src/lib/supabase/database.types.ts` returns at least 1
    - El push a Supabase no devolvió error (exit code 0)
  </acceptance_criteria>
  <done>
    Supabase DB reflejada con las dos tablas nuevas y database.types.ts incluye `matches` y `match_scoring_events` en el tipo `Database['public']['Tables']`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Crear utilidades de dominio, schemas zod, CSV parser y queries server-side</name>
  <files>
    src/lib/matches/schema.ts,
    src/lib/matches/utils.ts,
    src/lib/matches/utils.test.ts,
    src/lib/matches/csv-parser.ts,
    src/lib/matches/csv-parser.test.ts,
    src/lib/queries/matches.ts
  </files>
  <read_first>
    - src/lib/players/schema.ts (patrón zod exacto)
    - src/lib/queries/players.ts (patrón query server-side exacto)
    - src/lib/supabase/database.types.ts (tipos de matches recién regenerados — verificar nombres exactos de columnas)
    - .planning/phases/02-fixture-management/02-RESEARCH.md (§ Code Examples — CSV parser y getMatchResult al pie de la letra)
    - fixture-virreyes-2026.csv (verificar formato real — 7 columnas, comas simples, sin quotes)
  </read_first>
  <behavior>
    Tests para utils.ts:
    - getMatchResult(null, null, '2026-04-11') === 'pending'
    - getMatchResult(24, 17, '2026-04-11') === 'won'
    - getMatchResult(10, 21, '2026-04-11') === 'lost'
    - getMatchResult(15, 15, '2026-04-11') === 'draw'
    - formatMatchDate('2026-04-11') incluye 'sáb' o 'Sáb' y '11/04' (es-AR)

    Tests para csv-parser.ts:
    - parseFixtureCSV('') devuelve { matches: [], errors: [{row: 0, message: 'CSV vacío o sin datos'}] }
    - parseFixtureCSV('division,fecha_nro,fecha,equipo,local_visitante,rival,hora\nM19,1,2026-04-11,A,Local,Banco Provincia,16:00') devuelve 1 match parseado con division='M19', fecha_nro=1, equipo='A'
    - parseFixtureCSV con división inválida ('M14') devuelve error en errors[]
    - parseFixtureCSV con local_visitante inválido ('Casa') devuelve error en errors[]
    - parseFixtureCSV normaliza CRLF de Windows: 'header\r\nM19,1,2026-04-11,,Local,X,16:00\r\n' parsea correctamente sin \r en los valores
    - parseFixtureCSV con fila de 5 columnas en lugar de 7 reporta error de columnas
  </behavior>
  <action>
**Paso 1 — crear `src/lib/matches/schema.ts`:**

```typescript
import { z } from 'zod'

export const MatchFormSchema = z.object({
  division_id: z.string().min(1, 'División requerida'),
  match_date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/, 'Fecha inválida'),
  match_time: z.string().regex(/^\d{2}:\d{2}(:\d{2})?$/, 'Hora inválida').optional().or(z.literal('')),
  fecha_nro: z.number().int().min(1).max(50).optional().nullable(),
  rival: z.string().trim().min(1, 'Rival requerido').max(120),
  home_away: z.enum(['local', 'visitante']),
  venue: z.string().trim().max(120).optional().or(z.literal('')),
  subequipo: z.string().trim().max(4).optional().or(z.literal('')),
})

export type MatchFormInput = z.infer<typeof MatchFormSchema>

export const MatchResultSchema = z.object({
  score_home: z.number().int().min(0).max(200).nullable(),
  score_away: z.number().int().min(0).max(200).nullable(),
}).refine(
  (v) => (v.score_home === null && v.score_away === null) || (v.score_home !== null && v.score_away !== null),
  { message: 'Ambos puntajes deben estar cargados o ambos vacíos' }
)

export type MatchResultInput = z.infer<typeof MatchResultSchema>

export const ScoringEventSchema = z.object({
  match_id: z.string().min(1),
  team: z.enum(['home', 'away']),
  event_type: z.enum(['try', 'conversion', 'penalty', 'drop', 'yellow_card', 'red_card']),
  player_id: z.string().min(1).optional().nullable(),
  rival_scorer: z.string().trim().max(120).optional().nullable(),
})

export type ScoringEventInput = z.infer<typeof ScoringEventSchema>
```

**Paso 2 — crear `src/lib/matches/utils.ts`:**

```typescript
export type MatchResult = 'won' | 'lost' | 'draw' | 'pending'

export function getMatchResult(
  scoreHome: number | null,
  scoreAway: number | null,
  _matchDate: string
): MatchResult {
  if (scoreHome === null || scoreAway === null) return 'pending'
  if (scoreHome > scoreAway) return 'won'
  if (scoreHome < scoreAway) return 'lost'
  return 'draw'
}

export function formatMatchDate(isoDate: string): string {
  // 'YYYY-MM-DD' -> 'Sáb 11/04' (es-AR)
  const date = new Date(isoDate + 'T12:00:00')
  return date.toLocaleDateString('es-AR', { weekday: 'short', day: '2-digit', month: '2-digit' })
}
```

**Paso 3 — crear `src/lib/matches/utils.test.ts`:**

```typescript
import { describe, it, expect } from 'vitest'
import { getMatchResult, formatMatchDate } from './utils'

describe('getMatchResult', () => {
  it('returns pending when no scores', () => {
    expect(getMatchResult(null, null, '2026-04-11')).toBe('pending')
  })
  it('returns won when home > away', () => {
    expect(getMatchResult(24, 17, '2026-04-11')).toBe('won')
  })
  it('returns lost when home < away', () => {
    expect(getMatchResult(10, 21, '2026-04-11')).toBe('lost')
  })
  it('returns draw when home === away', () => {
    expect(getMatchResult(15, 15, '2026-04-11')).toBe('draw')
  })
})

describe('formatMatchDate', () => {
  it('formats ISO date into es-AR short weekday + DD/MM', () => {
    const out = formatMatchDate('2026-04-11')
    expect(out.toLowerCase()).toMatch(/sáb|sab/) // tolerate accent variation
    expect(out).toContain('11/04')
  })
})
```

**Paso 4 — crear `src/lib/matches/csv-parser.ts` (copiar de RESEARCH.md § Code Examples):**

```typescript
export type ParsedMatch = {
  division: string
  fecha_nro: number
  fecha: string
  equipo: string | null
  local_visitante: string
  rival: string
  hora: string | null
}

export type CsvParseResult = {
  matches: ParsedMatch[]
  errors: { row: number; message: string }[]
}

const VALID_DIVISIONS = new Set(['M15', 'M16', 'M17', 'M19'])
const VALID_HOME_AWAY = new Set(['Local', 'Visitante'])

export function parseFixtureCSV(text: string): CsvParseResult {
  const normalized = text.replace(/\r\n/g, '\n').replace(/\r/g, '\n')
  const lines = normalized.split('\n').filter((l) => l.trim().length > 0)

  if (lines.length < 2) {
    return { matches: [], errors: [{ row: 0, message: 'CSV vacío o sin datos' }] }
  }

  const [, ...rows] = lines
  const matches: ParsedMatch[] = []
  const errors: { row: number; message: string }[] = []

  rows.forEach((line, idx) => {
    const rowNum = idx + 2
    const cols = line.split(',')
    if (cols.length !== 7) {
      errors.push({ row: rowNum, message: `Se esperaban 7 columnas, se encontraron ${cols.length}` })
      return
    }
    const [division, fecha_nro_str, fecha, equipo, local_visitante, rival, hora] = cols.map((c) => c.trim())

    if (!VALID_DIVISIONS.has(division)) {
      errors.push({ row: rowNum, message: `División inválida: "${division}"` })
      return
    }
    if (!VALID_HOME_AWAY.has(local_visitante)) {
      errors.push({ row: rowNum, message: `Local/Visitante inválido: "${local_visitante}"` })
      return
    }

    const fecha_nro = parseInt(fecha_nro_str, 10)
    if (isNaN(fecha_nro)) {
      errors.push({ row: rowNum, message: `Fecha N° inválida: "${fecha_nro_str}"` })
      return
    }

    matches.push({
      division,
      fecha_nro,
      fecha,
      equipo: equipo || null,
      local_visitante,
      rival,
      hora: hora || null,
    })
  })

  return { matches, errors }
}
```

**Paso 5 — crear `src/lib/matches/csv-parser.test.ts`:**

```typescript
import { describe, it, expect } from 'vitest'
import { parseFixtureCSV } from './csv-parser'

const HEADER = 'division,fecha_nro,fecha,equipo,local_visitante,rival,hora'

describe('parseFixtureCSV', () => {
  it('returns empty + error on empty input', () => {
    const r = parseFixtureCSV('')
    expect(r.matches).toHaveLength(0)
    expect(r.errors[0].message).toMatch(/vacío|datos/i)
  })

  it('parses a single valid row', () => {
    const csv = `${HEADER}\nM19,1,2026-04-11,A,Local,Banco Provincia,16:00`
    const r = parseFixtureCSV(csv)
    expect(r.errors).toHaveLength(0)
    expect(r.matches).toHaveLength(1)
    expect(r.matches[0]).toEqual({
      division: 'M19',
      fecha_nro: 1,
      fecha: '2026-04-11',
      equipo: 'A',
      local_visitante: 'Local',
      rival: 'Banco Provincia',
      hora: '16:00',
    })
  })

  it('reports invalid division', () => {
    const csv = `${HEADER}\nM14,1,2026-04-11,,Local,X,16:00`
    const r = parseFixtureCSV(csv)
    expect(r.matches).toHaveLength(0)
    expect(r.errors[0].message).toMatch(/División inválida/)
  })

  it('reports invalid local_visitante', () => {
    const csv = `${HEADER}\nM19,1,2026-04-11,,Casa,X,16:00`
    const r = parseFixtureCSV(csv)
    expect(r.matches).toHaveLength(0)
    expect(r.errors[0].message).toMatch(/Local\/Visitante/)
  })

  it('normalizes CRLF Windows line endings', () => {
    const csv = `${HEADER}\r\nM19,1,2026-04-11,,Local,X,16:00\r\n`
    const r = parseFixtureCSV(csv)
    expect(r.errors).toHaveLength(0)
    expect(r.matches[0].hora).toBe('16:00')
  })

  it('reports wrong column count', () => {
    const csv = `${HEADER}\nM19,1,2026-04-11,A,Local`
    const r = parseFixtureCSV(csv)
    expect(r.errors[0].message).toMatch(/7 columnas/)
  })

  it('parses empty equipo as null', () => {
    const csv = `${HEADER}\nM15,1,2026-04-11,,Local,X,16:00`
    const r = parseFixtureCSV(csv)
    expect(r.matches[0].equipo).toBeNull()
  })
})
```

**Paso 6 — crear `src/lib/queries/matches.ts` siguiendo el patrón de `players.ts`:**

```typescript
import 'server-only'
import { createClient } from '@/lib/supabase/server'
import type { Database } from '@/lib/supabase/database.types'

type MatchRow = Database['public']['Tables']['matches']['Row']
type ScoringEventRow = Database['public']['Tables']['match_scoring_events']['Row']

export type MatchWithDivision = MatchRow & {
  divisions: { name: string } | null
}

export type MatchWithEvents = MatchRow & {
  match_scoring_events: ScoringEventRow[]
}

export async function listByDivision(divisionId: string): Promise<MatchRow[]> {
  const supabase = createClient()
  const { data, error } = await supabase
    .from('matches')
    .select('*')
    .eq('division_id', divisionId)
    .order('match_date', { ascending: true })
    .order('match_time', { ascending: true })
  if (error) throw error
  return (data ?? []) as MatchRow[]
}

export async function listAllJuvenile(): Promise<MatchWithDivision[]> {
  const supabase = createClient()
  // Join divisions; filter to is_juvenile via divisions FK
  const { data, error } = await supabase
    .from('matches')
    .select('*, divisions!inner(name, is_juvenile)')
    .eq('divisions.is_juvenile', true)
    .order('match_date', { ascending: true })
    .order('match_time', { ascending: true })
  if (error) throw error
  return (data ?? []) as unknown as MatchWithDivision[]
}

export async function getById(matchId: string): Promise<MatchRow | null> {
  const supabase = createClient()
  const { data, error } = await supabase
    .from('matches')
    .select('*')
    .eq('id', matchId)
    .maybeSingle()
  if (error) throw error
  return (data as MatchRow) ?? null
}

export async function getWithEvents(matchId: string): Promise<MatchWithEvents | null> {
  const supabase = createClient()
  const { data, error } = await supabase
    .from('matches')
    .select('*, match_scoring_events(*)')
    .eq('id', matchId)
    .maybeSingle()
  if (error) throw error
  return (data as unknown as MatchWithEvents) ?? null
}
```

NOTA: si TypeScript se queja porque `database.types.ts` no incluye aún `matches` (Task 2 falló), detener y regresar a Task 2 antes de continuar.
  </action>
  <verify>
    <automated>npx vitest run src/lib/matches --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/matches/schema.ts` exists and `grep -c "MatchFormSchema" src/lib/matches/schema.ts` returns at least 1
    - File `src/lib/matches/schema.ts` contains `ScoringEventSchema`, `MatchResultSchema`
    - File `src/lib/matches/utils.ts` exports `getMatchResult` and `formatMatchDate` (grep both)
    - File `src/lib/matches/csv-parser.ts` exports `parseFixtureCSV`
    - File `src/lib/queries/matches.ts` starts with `import 'server-only'`
    - File `src/lib/queries/matches.ts` exports `listByDivision`, `listAllJuvenile`, `getById`, `getWithEvents`
    - `npx vitest run src/lib/matches` exits 0 with all tests passing (utils + csv-parser)
    - `npx tsc --noEmit` runs without errors related to `src/lib/matches` or `src/lib/queries/matches.ts`
  </acceptance_criteria>
  <done>
    schema.ts, utils.ts, csv-parser.ts, queries/matches.ts existen, tests verdes, build typechecks limpio en estos archivos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Actions | El input del coach (form de partido, scoring events) cruza esta frontera |
| client → API route /api/fixture/import | El archivo CSV cargado por admin (Plan 05) cruza esta frontera |
| Supabase RLS | Frontera última de autorización — coach no puede leer/escribir matches fuera de sus divisiones |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | matches table | mitigate | RLS policy `matches_read/insert/update` requires JOIN con coach_divisions o role=admin |
| T-02-02 | Tampering | matches table | mitigate | CHECK constraint `matches_score_consistency` impide guardar parcialmente el resultado (score_home sin score_away) |
| T-02-03 | Tampering | matches.delete | mitigate | RLS policy `matches_delete` solo permite admin (D-16) — coach no puede borrar |
| T-02-04 | Information Disclosure | match_scoring_events | mitigate | RLS JOIN con matches replica permiso del partido padre |
| T-02-05 | Tampering | parseFixtureCSV | mitigate | Validación estricta de divisiones (Set whitelist) y home_away (Set whitelist); número de columnas validado |
| T-02-06 | Denial of Service | parseFixtureCSV | accept | El parser corre server-side con CSV de ~60 filas; sin loops anidados; riesgo bajo |
| T-02-07 | Tampering | match_scoring_events.player_id | accept | FK ON DELETE SET NULL preserva integridad histórica; Plan 04 validará que player_id pertenezca a la división del partido |
</threat_model>

<verification>
- `supabase db push` ejecutado sin error
- `npx tsc --noEmit` pasa sin errores nuevos
- `npx vitest run src/lib/matches` pasa con todos los tests verdes
- `grep "matches:" src/lib/supabase/database.types.ts` confirma que la regeneración de tipos incluyó la nueva tabla
- Migration file existe en `supabase/migrations/`
</verification>

<success_criteria>
- DB tiene las tablas `public.matches` y `public.match_scoring_events` con RLS activa
- `database.types.ts` regenerado e incluye ambas tablas
- `src/lib/matches/{schema,utils,csv-parser}.ts` + tests verdes
- `src/lib/queries/matches.ts` exporta las 4 queries requeridas
- Plans 02-05 pueden importar tipos, schemas, utilidades, parser y queries sin necesidad de más fundación
</success_criteria>

<output>
After completion, create `.planning/phases/02-fixture-management/02-01-SUMMARY.md`.
</output>
</content>
</invoke>