---
phase: 02
plan: 05
type: execute
wave: 5
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - src/app/(app)/admin/fixture-import/page.tsx
  - src/app/(app)/admin/fixture-import/actions.ts
  - src/app/(app)/admin/fixture-import/FixtureImportClient.tsx
autonomous: true
requirements: [FIX-01, FIX-02, ADM-03]
must_haves:
  truths:
    - "Un admin abre /admin/fixture-import y ve un formulario para subir un archivo CSV"
    - "Al subir el CSV, el admin ve una tabla de preview con los partidos parseados antes de confirmar"
    - "Las filas con error de parseo se muestran en la preview diferenciadas (no se importan)"
    - "Al tocar 'Confirmar import' los partidos URBA se reemplazan (truncate de manual=false + insert) en las divisiones del CSV"
    - "Los partidos manuales (manual=true) se preservan durante el reimport"
    - "Un coach o tutora que intenta acceder a /admin/fixture-import es redirigido (no admin)"
    - "El link 'Importar fixture' de EmptyFixture llega a una ruta existente (ya no es 404)"
  artifacts:
    - path: "src/app/(app)/admin/fixture-import/page.tsx"
      provides: "Server Component admin-only que carga las divisiones juveniles y monta el cliente de import"
      contains: "redirect('/jugadores')"
    - path: "src/app/(app)/admin/fixture-import/actions.ts"
      provides: "Server Action de import: parse + truncate manual=false + insert, admin-only"
      exports: ["confirmFixtureImport"]
    - path: "src/app/(app)/admin/fixture-import/FixtureImportClient.tsx"
      provides: "Client Component: upload CSV, preview table, confirmar"
      contains: "parseFixtureCSV"
  key_links:
    - from: "src/app/(app)/admin/fixture-import/FixtureImportClient.tsx"
      to: "src/lib/matches/csv-parser.ts"
      via: "import parseFixtureCSV"
      pattern: "parseFixtureCSV"
    - from: "src/app/(app)/admin/fixture-import/FixtureImportClient.tsx"
      to: "src/app/(app)/admin/fixture-import/actions.ts"
      via: "import confirmFixtureImport"
      pattern: "confirmFixtureImport"
    - from: "src/app/(app)/admin/fixture-import/actions.ts"
      to: "matches table"
      via: "delete manual=false + insert"
      pattern: "\\.eq\\('manual', false\\)"
---

<objective>
Cerrar el gap de verificación de Fase 2: construir la UI de import del fixture URBA que faltaba (FIX-01, FIX-02, ADM-03). El parser `parseFixtureCSV` ya existe con 12 tests, las tablas `matches` ya existen, pero no hay página de import, ni preview, ni Server Action de truncate+insert. El link "Importar fixture" en `EmptyFixture.tsx` apunta a `/admin/fixture-import` que hoy es 404.

Purpose: Sin esta UI ningún admin puede cargar el fixture URBA de ~60 partidos por la app, y el `parseFixtureCSV` queda idle. Es el único gap que bloquea el success criterion 3 del ROADMAP ("Admin can upload a URBA CSV file and see a preview of parsed matches before confirming the import").

Output: Ruta `/admin/fixture-import` admin-only con flujo upload → preview → confirmar. El parse corre en el cliente (el parser es puro y sin dependencias de Node), la preview muestra los partidos y errores, y "Confirmar import" llama una Server Action que hace truncate de los partidos URBA (manual=false) de las divisiones presentes en el CSV e inserta los nuevos, preservando los partidos manuales (D-12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fixture-management/02-CONTEXT.md
@.planning/phases/02-fixture-management/02-UI-SPEC.md
@.planning/phases/02-fixture-management/02-VERIFICATION.md
@CLAUDE.md
@fixture-virreyes-2026.csv

<interfaces>
<!-- Contratos existentes que el executor consume. NO explorar el codebase: usar esto. -->

From src/lib/matches/csv-parser.ts (YA EXISTE — no modificar):
```typescript
export type ParsedMatch = {
  division: string            // 'M15' | 'M16' | 'M17' | 'M19' (= divisions.name)
  fecha_nro: number
  fecha: string               // 'YYYY-MM-DD' (validado)
  equipo: string | null       // subequipo, ej. 'A' o null
  local_visitante: string     // 'Local' | 'Visitante' (validado)
  rival: string
  hora: string | null         // ej. '16:00' o null
}
export type CsvParseResult = {
  matches: ParsedMatch[]
  errors: { row: number; message: string }[]
}
export function parseFixtureCSV(text: string): CsvParseResult
```
El parser es una función pura sin imports — se puede llamar desde un Client Component directamente.

From src/lib/supabase/database.types.ts (YA EXISTE):
```typescript
// matches Insert shape (campos relevantes para el import):
type MatchInsert = Database['public']['Tables']['matches']['Insert']
// { division_id: string; match_date: string; match_time?: string|null;
//   fecha_nro?: number|null; rival: string;
//   home_away: 'local'|'visitante'; venue?: string|null;
//   subequipo?: string|null; manual?: boolean; created_by?: string|null }
// divisions Row: { id: string; name: string; is_juvenile: boolean; ... }
```

From src/lib/supabase/server.ts (YA EXISTE):
```typescript
import { createClient } from '@/lib/supabase/server'  // RLS-aware server client
```

From src/app/(app)/admin/actions.ts (PATRÓN admin-only a replicar EXACTO):
```typescript
async function requireAdmin() {
  const supabase = createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) throw new Error('No autorizado')
  const { data: profileData } = await supabase
    .from('profiles').select('role').eq('id', user.id).single()
  const profile = profileData as { role: 'admin'|'coach'|'tutora' } | null
  if (profile?.role !== 'admin')
    throw new Error('Solo administradores pueden ejecutar esta acción')
  return { supabase, user }
}
```

From src/app/(app)/admin/page.tsx (PATRÓN de página admin-only a replicar EXACTO):
```typescript
const { data: { user } } = await supabase.auth.getUser()
if (!user) redirect('/login')
const { data: profileData } = await supabase
  .from('profiles').select('role').eq('id', user.id).single()
const profile = profileData as { role: 'admin'|'coach'|'tutora' } | null
if (profile?.role !== 'admin') redirect('/jugadores')
// luego: supabase.from('divisions').select('id, name').eq('is_juvenile', true).order('name')
```

From src/app/(app)/fixture/actions.ts (PATRÓN de mutación con cast `(supabase as any)`):
```typescript
// eslint-disable-next-line @typescript-eslint/no-explicit-any
const { data, error } = await (supabase as any)
  .from('matches').insert(insertPayload).select('id').single()
```

UI: Toast vía `sonner` (ya instalado). NO usar el componente shadcn `Table` (no está instalado en este repo) — renderizar la preview con un `<table>` HTML estilado con Tailwind según el UI-SPEC.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Server Action confirmFixtureImport (parse + truncate manual=false + insert, admin-only)</name>
  <files>src/app/(app)/admin/fixture-import/actions.ts</files>
  <read_first>
    - src/app/(app)/admin/actions.ts (patrón requireAdmin EXACTO a replicar)
    - src/app/(app)/fixture/actions.ts (patrón de insert con cast `(supabase as any)` y revalidatePath)
    - src/lib/matches/csv-parser.ts (firma de parseFixtureCSV, ParsedMatch, CsvParseResult)
    - src/lib/supabase/database.types.ts (shape de matches Insert y divisions Row)
    - src/lib/supabase/server.ts (createClient)
    - .planning/phases/02-fixture-management/02-CONTEXT.md (D-12 truncate+insert preservando manual=true; D-13 admin-only)
  </read_first>
  <action>
Crear `src/app/(app)/admin/fixture-import/actions.ts` con una Server Action `confirmFixtureImport` que recibe el texto crudo del CSV (string), lo re-parsea server-side (NO confiar en el parse del cliente — el cliente puede manipularse), verifica admin, y ejecuta truncate de partidos URBA + insert.

Reglas concretas:
- `'use server'` en la primera línea.
- Replicar `requireAdmin()` EXACTAMENTE como en `src/app/(app)/admin/actions.ts` (mismo cuerpo, mismos mensajes de error). Devuelve `{ supabase, user }`.
- Re-parsear con `parseFixtureCSV(csvText)` server-side. Si `result.matches.length === 0`, lanzar `new Error('No hay partidos válidos para importar')`.
- Mapear cada `ParsedMatch` a un `matches` Insert. Para resolver `division_id` desde `ParsedMatch.division` (que es el `name`, ej. 'M19'):
  1. Cargar las divisiones juveniles: `(supabase as any).from('divisions').select('id, name').eq('is_juvenile', true)`.
  2. Construir un mapa `name -> id`. Si una división del CSV no existe en el mapa, acumular su nombre en un array `unknownDivisions` y saltear esa fila.
  3. Si `unknownDivisions.length > 0` después de mapear todo, lanzar `new Error('Divisiones no encontradas en la base: ' + Array.from(new Set(unknownDivisions)).join(', '))` ANTES de cualquier delete (no hacer truncate parcial).
- Mapeo de campos por fila:
  - `division_id` = mapa[m.division]
  - `match_date` = m.fecha
  - `match_time` = m.hora (ya viene null o 'HH:MM')
  - `fecha_nro` = m.fecha_nro
  - `rival` = m.rival
  - `home_away` = m.local_visitante === 'Local' ? 'local' : 'visitante'  (lowercase — el CHECK constraint exige 'local'/'visitante')
  - `venue` = null
  - `subequipo` = m.equipo
  - `manual` = false  (es URBA — debe ser borrable en el próximo reimport)
  - `created_by` = user.id
- Truncate (preservando manuales, D-12): determinar el set de `division_id` distintos que aparecen en el CSV mapeado. Borrar SOLO los partidos URBA de esas divisiones:
  ```typescript
  const divisionIds = Array.from(new Set(rows.map((r) => r.division_id)))
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { error: delError } = await (supabase as any)
    .from('matches')
    .delete()
    .eq('manual', false)
    .in('division_id', divisionIds)
  if (delError) throw new Error('No se pudieron borrar los partidos URBA previos: ' + delError.message)
  ```
  El `.eq('manual', false)` garantiza que los partidos cargados a mano (manual=true) se preservan. El `.in('division_id', divisionIds)` limita el borrado a las divisiones presentes en este CSV.
- Insert masivo de las filas nuevas:
  ```typescript
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { error: insError } = await (supabase as any)
    .from('matches').insert(rows)
  if (insError) throw new Error('No se pudieron insertar los partidos: ' + insError.message)
  ```
- `revalidatePath('/fixture')` y `revalidatePath('/admin/fixture-import')` al final.
- Retornar `{ imported: rows.length }`.

Nota de seguridad/transaccionalidad: el delete + insert NO es atómico vía el client REST de Supabase. Para mantener el alcance del gap acotado, ejecutar delete y luego insert secuencialmente; si el insert falla, lanzar el error (el admin reintenta). NO introducir una RPC/función SQL nueva en este plan (eso sería expandir el alcance). Documentar este trade-off con un comentario en el código: `// Trade-off: delete+insert no es atómico; si el insert falla el admin reintenta el import completo.`

Firma final:
```typescript
export async function confirmFixtureImport(csvText: string): Promise<{ imported: number }>
```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/(app)/admin/fixture-import/actions.ts` exists and first line is `'use server'`
    - `grep -c "export async function confirmFixtureImport" "src/app/(app)/admin/fixture-import/actions.ts"` returns 1
    - `grep -c "parseFixtureCSV" "src/app/(app)/admin/fixture-import/actions.ts"` returns at least 1
    - `grep -c "profile?.role !== 'admin'" "src/app/(app)/admin/fixture-import/actions.ts"` returns at least 1
    - `grep -c "\.eq('manual', false)" "src/app/(app)/admin/fixture-import/actions.ts"` returns 1 (truncate preserva manuales)
    - `grep -c "\.in('division_id'" "src/app/(app)/admin/fixture-import/actions.ts"` returns 1 (truncate limitado a divisiones del CSV)
    - `grep -c "manual: false" "src/app/(app)/admin/fixture-import/actions.ts"` returns at least 1 (los importados son URBA)
    - `grep -c "is_juvenile" "src/app/(app)/admin/fixture-import/actions.ts"` returns at least 1 (resuelve division_id solo de juveniles)
    - `grep -c "home_away" "src/app/(app)/admin/fixture-import/actions.ts"` returns at least 1 y el archivo mapea 'Local' a 'local' (lowercase) — `grep -c "'Local' ? 'local'" "src/app/(app)/admin/fixture-import/actions.ts"` returns at least 1
    - `grep -c "revalidatePath('/fixture')" "src/app/(app)/admin/fixture-import/actions.ts"` returns 1
    - El archivo NO crea ninguna migración ni función SQL nueva (no contiene 'CREATE TABLE' ni 'CREATE FUNCTION')
    - `npx tsc --noEmit` corre sin errores nuevos relacionados con este archivo
  </acceptance_criteria>
  <done>
    confirmFixtureImport existe, es admin-only, re-parsea server-side, resuelve division_id por name juvenil, hace delete de manual=false en las divisiones del CSV (preservando manuales) e inserta los nuevos partidos URBA; revalida /fixture.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Client Component de import (upload → preview → confirmar)</name>
  <files>src/app/(app)/admin/fixture-import/FixtureImportClient.tsx</files>
  <read_first>
    - src/lib/matches/csv-parser.ts (parseFixtureCSV, ParsedMatch, CsvParseResult — firma exacta)
    - src/app/(app)/admin/fixture-import/actions.ts (Task 1 — confirmFixtureImport firma)
    - .planning/phases/02-fixture-management/02-UI-SPEC.md (§ CSV Import UI: Step 1 upload, Step 2 preview, Step 3 confirm; § Copywriting Contract; § Interaction States)
    - src/components/ui/button.tsx (export Button + variantes 'ghost')
    - src/components/ui/input.tsx (export Input)
    - src/components/ui/card.tsx (Card, CardContent, CardHeader, CardTitle)
    - src/components/ui/sonner.tsx (toast pattern — `import { toast } from 'sonner'`)
    - src/components/fixture/ResultBadge.tsx (referencia de estilo de badges Tailwind, no se reusa)
  </read_first>
  <action>
Crear `src/app/(app)/admin/fixture-import/FixtureImportClient.tsx` — Client Component (`'use client'` primera línea). Implementa el flujo de 3 pasos del UI-SPEC.

Estado interno (useState):
- `step: 'upload' | 'preview'`
- `csvText: string` (el texto crudo del archivo leído)
- `parseResult: CsvParseResult | null`
- `submitting: boolean`

Paso 1 — Upload (cuando `step === 'upload'`):
- `Card` con `CardHeader > CardTitle` "Importar fixture URBA" (`text-xl font-semibold`).
- Helper text exacto: `"Formato CSV con columnas: division, fecha_nro, fecha, equipo, local_visitante, rival, hora"` en `text-sm text-muted-foreground`.
- `<input type="file" accept=".csv" aria-label="Archivo CSV del fixture" />` (Input shadcn o input nativo estilado).
- Al seleccionar archivo: leer con `await file.text()`, guardar en `csvText`, llamar `parseFixtureCSV(text)` y guardar en `parseResult`, y pasar `step = 'preview'`.
- Si la lectura falla, `toast.error('No se pudo leer el archivo. Verificá que sea un CSV con el formato correcto.')`.

Paso 2 — Preview (cuando `step === 'preview'` y `parseResult` no es null):
- Resumen arriba de la tabla: `"Se encontraron {parseResult.matches.length} partidos. {parseResult.errors.length} errores."` en `text-sm text-muted-foreground`.
- Tabla HTML (`<table className="w-full text-sm">`) — NO usar el componente shadcn Table (no instalado). Columnas: División, Fecha #, Fecha, Equipo, L/V, Rival, Hora. Filas de `parseResult.matches` con `min-h` de 48px por fila (`className="h-12"` en los `<tr>` o padding en celdas).
- Si `parseResult.errors.length > 0`: renderizar una sección de errores debajo (o filas marcadas) con `bg-destructive/20 text-destructive`, mostrando `row` y `message` de cada error.
- Warning antes del botón confirmar (siempre que haya algo para importar): `"Esto reemplazará todos los partidos URBA importados. Los partidos manuales se preservan."` en `text-sm text-amber-400`.
- Botón "Confirmar import" — primary, full-width, `h-12`. Deshabilitado si `parseResult.matches.length === 0` o `submitting`. Texto "Importando…" mientras `submitting`.
- Botón "Cancelar" — `variant="ghost"`, full-width — vuelve a `step = 'upload'` y limpia `parseResult`/`csvText`.

Confirmar (onClick del botón confirmar):
- `setSubmitting(true)`, en try: `const res = await confirmFixtureImport(csvText)`.
- En éxito: `toast.success(`Fixture importado: ${res.imported} partidos cargados`)`, reset a `step='upload'` y limpiar estado, y `router.push('/fixture')` (usar `useRouter` de `next/navigation`).
- En catch (e): `toast.error('Falló la importación. Revisá los errores en la tabla y volvé a intentarlo.')` (opcionalmente incluir e.message si existe).
- En finally: `setSubmitting(false)`.

Copy exacto (del UI-SPEC Copywriting Contract):
- CTA upload: "Subir archivo" (si se usa botón intermedio; con input file directo el label puede ser "Elegí un archivo CSV").
- CTA confirmar: "Confirmar import"
- CTA cancelar: "Cancelar"
- Submitting: "Importando…"
- Toast éxito: "Fixture importado: N partidos cargados"
- Error parse: "No se pudo leer el archivo. Verificá que sea un CSV con el formato correcto."
- Error import: "Falló la importación. Revisá los errores en la tabla y volvé a intentarlo."
- Warning replace: "Esto reemplazará todos los partidos URBA importados. Los partidos manuales se preservan."

Props del componente: ninguna requerida para el flujo (el parse es client-side). Firma: `export function FixtureImportClient()`.

Colores/tailwind: usar `var(--background)`/`var(--card)` vía clases shadcn (`bg-card`), el botón confirmar usa el verde primary por default (Button default variant). No hardcodear hex.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/(app)/admin/fixture-import/FixtureImportClient.tsx` exists and first line is `'use client'`
    - `grep -c "import { parseFixtureCSV" "src/app/(app)/admin/fixture-import/FixtureImportClient.tsx"` returns 1
    - `grep -c "confirmFixtureImport" "src/app/(app)/admin/fixture-import/FixtureImportClient.tsx"` returns at least 1
    - `grep -c "accept=\".csv\"" "src/app/(app)/admin/fixture-import/FixtureImportClient.tsx"` returns 1
    - `grep -c "Confirmar import" "src/app/(app)/admin/fixture-import/FixtureImportClient.tsx"` returns at least 1
    - `grep -c "Importando…" "src/app/(app)/admin/fixture-import/FixtureImportClient.tsx"` returns at least 1
    - `grep -c "Se encontraron" "src/app/(app)/admin/fixture-import/FixtureImportClient.tsx"` returns 1 (resumen de preview)
    - `grep -c "Los partidos manuales se preservan" "src/app/(app)/admin/fixture-import/FixtureImportClient.tsx"` returns 1 (warning)
    - `grep -c "bg-destructive/20" "src/app/(app)/admin/fixture-import/FixtureImportClient.tsx"` returns at least 1 (filas/errores diferenciados)
    - `grep -c "Fixture importado:" "src/app/(app)/admin/fixture-import/FixtureImportClient.tsx"` returns 1 (toast éxito)
    - El archivo NO importa el componente shadcn `Table` (`grep -c "@/components/ui/table" ...` returns 0)
    - `npx tsc --noEmit` corre sin errores nuevos
  </acceptance_criteria>
  <done>
    FixtureImportClient implementa upload → preview (tabla con partidos + errores) → confirmar. El parse es client-side; confirmar llama la Server Action; toasts y warning con el copy del UI-SPEC; navega a /fixture al terminar.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Página admin-only /admin/fixture-import + verificar link de EmptyFixture</name>
  <files>src/app/(app)/admin/fixture-import/page.tsx</files>
  <read_first>
    - src/app/(app)/admin/page.tsx (patrón de página admin-only con redirect('/jugadores') a replicar)
    - src/app/(app)/admin/fixture-import/FixtureImportClient.tsx (Task 2 — componente a montar)
    - src/components/fixture/EmptyFixture.tsx (verificar que el link href="/admin/fixture-import" ahora resuelve)
    - src/lib/supabase/server.ts (createClient)
  </read_first>
  <action>
Crear `src/app/(app)/admin/fixture-import/page.tsx` — Server Component (sin `'use client'`). Replicar el guard admin-only de `src/app/(app)/admin/page.tsx`:

```typescript
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { FixtureImportClient } from './FixtureImportClient'

export default async function FixtureImportPage() {
  const supabase = createClient()

  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const { data: profileData } = await supabase
    .from('profiles')
    .select('role')
    .eq('id', user.id)
    .single()

  const profile = profileData as { role: 'admin' | 'coach' | 'tutora' } | null
  if (profile?.role !== 'admin') {
    redirect('/jugadores')
  }

  return (
    <div className="p-4 space-y-6">
      <h1 className="text-xl font-semibold">Importar fixture URBA</h1>
      <FixtureImportClient />
    </div>
  )
}
```

Sobre `EmptyFixture.tsx`: el link `href="/admin/fixture-import"` ya existe (línea 18). NO modificar EmptyFixture — al crear esta ruta el link deja de ser 404 automáticamente. La verificación abajo confirma que el link sigue apuntando a la ruta correcta y que la ruta existe.

NO crear un layout adicional ni middleware — el guard inline (igual que en /admin/page.tsx) es suficiente y consistente con el resto del codebase.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/(app)/admin/fixture-import/page.tsx` exists and does NOT contain `'use client'`
    - `grep -c "redirect('/jugadores')" "src/app/(app)/admin/fixture-import/page.tsx"` returns 1 (guard admin-only)
    - `grep -c "redirect('/login')" "src/app/(app)/admin/fixture-import/page.tsx"` returns 1
    - `grep -c "profile?.role !== 'admin'" "src/app/(app)/admin/fixture-import/page.tsx"` returns 1
    - `grep -c "FixtureImportClient" "src/app/(app)/admin/fixture-import/page.tsx"` returns at least 1 (montado)
    - `grep -c "href=\"/admin/fixture-import\"" src/components/fixture/EmptyFixture.tsx` returns 1 (el link sigue apuntando a la ruta que ahora existe)
    - `npm run build` succeeds — la ruta `/admin/fixture-import` compila sin error y aparece en el output del build
  </acceptance_criteria>
  <done>
    La ruta /admin/fixture-import existe, es admin-only (redirect a /jugadores para no-admin), monta FixtureImportClient, y el link de EmptyFixture ya no es 404. El build de Next compila la ruta.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /admin/fixture-import (page) | Un coach/tutora podría intentar abrir la ruta directamente |
| client → confirmFixtureImport (Server Action) | El texto CSV cruza la frontera; un usuario no-admin podría invocar la action directamente |
| Server Action → matches table (delete + insert) | Operación destructiva (truncate) sobre datos de varias divisiones |
| Supabase RLS | Última frontera: las policies `matches_insert` y `matches_delete` (admin-only para delete) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Elevation of Privilege | /admin/fixture-import page | mitigate | Guard server-side `profile?.role !== 'admin' → redirect('/jugadores')`, idéntico a /admin/page.tsx; no se confía en el cliente |
| T-02-13 | Elevation of Privilege | confirmFixtureImport action | mitigate | `requireAdmin()` server-side lanza error si role !== 'admin' ANTES de cualquier mutación; además RLS `matches_delete` es admin-only |
| T-02-14 | Tampering | confirmFixtureImport input | mitigate | El CSV se RE-parsea server-side con `parseFixtureCSV` (whitelist de divisiones y home_away); no se confía en el parse del cliente |
| T-02-15 | Tampering / Destruction of data | matches truncate | mitigate | El delete está acotado con `.eq('manual', false)` (preserva partidos manuales, D-12) y `.in('division_id', divisionIds)` (solo divisiones presentes en el CSV); si hay divisiones desconocidas se aborta ANTES de borrar |
| T-02-16 | Tampering | división inexistente en CSV | mitigate | Se valida que cada `ParsedMatch.division` exista como `divisions.name` con `is_juvenile=true`; si no, se aborta sin tocar la DB |
| T-02-17 | Denial of Service | upload de archivo grande | accept | El fixture URBA es ~60 filas; el parser corre sin loops anidados; el navegador lee el archivo a memoria. Riesgo bajo para el caso de uso (admin de un club) |
| T-02-18 | Repudiation | quién importó el fixture | accept | `created_by = user.id` queda registrado en cada partido insertado; suficiente para trazabilidad básica en v1 |
| T-02-19 | Tampering (atomicidad) | delete+insert no transaccional | accept | Trade-off documentado en código: si el insert falla tras el delete, el admin reintenta el import completo. No se introduce RPC SQL para mantener el alcance del gap acotado |
</threat_model>

<verification>
- `npx tsc --noEmit` pasa sin errores nuevos
- `npm run build` compila la ruta `/admin/fixture-import`
- `grep "confirmFixtureImport" src/app/(app)/admin/fixture-import/FixtureImportClient.tsx` confirma el wiring cliente → action
- `grep "parseFixtureCSV" src/app/(app)/admin/fixture-import/actions.ts` confirma el re-parse server-side
- `grep ".eq('manual', false)" src/app/(app)/admin/fixture-import/actions.ts` confirma que el truncate preserva manuales
- El link de EmptyFixture (`/admin/fixture-import`) ya no es 404
- Verificación humana (requiere DB + sesión admin): subir `fixture-virreyes-2026.csv`, ver preview de ~60 partidos, confirmar, y comprobar que aparecen en /fixture agrupados por división y que un partido manual previo sobrevive
</verification>

<success_criteria>
- FIX-01 implementado: admin puede importar el fixture URBA desde un archivo CSV vía /admin/fixture-import
- FIX-02 implementado: el import muestra una tabla de preview de los partidos parseados (con errores diferenciados) antes de confirmar
- ADM-03 implementado: la ruta y la Server Action son exclusivas de admin (guard + requireAdmin + RLS)
- Success criterion 3 del ROADMAP queda satisfecho; el gap único de 02-VERIFICATION.md cerrado
- El link "Importar fixture" de EmptyFixture deja de ser 404
- D-12 respetado: truncate de manual=false + insert, preservando partidos manuales
</success_criteria>

<output>
After completion, create `.planning/phases/02-fixture-management/02-05-SUMMARY.md`.
</output>
</content>
</invoke>
