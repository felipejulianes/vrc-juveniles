---
phase: 02
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/context/DivisionContext.tsx
  - src/app/(app)/layout.tsx
  - src/components/layout/DivisionSelector.tsx
  - src/app/(app)/fixture/page.tsx
  - src/app/(app)/fixture/loading.tsx
  - src/components/fixture/MatchCard.tsx
  - src/components/fixture/ResultBadge.tsx
  - src/components/fixture/FixtureList.tsx
  - src/components/fixture/EmptyFixture.tsx
autonomous: true
requirements: [FIX-04, ADM-02]
must_haves:
  truths:
    - "Un coach abre /fixture y ve los partidos de su división activa ordenados por fecha"
    - "Un coach ve cards con fecha + rival + L/V + hora + badge de resultado (verde/rojo/gris)"
    - "Para M17 el card muestra el chip de subequipo ('A')"
    - "Un admin ve la opción 'Todas las divisiones' en el DivisionSelector"
    - "Al seleccionar 'Todas las divisiones' los cards se agrupan por división con header (M15, M16, M17, M19)"
    - "Un coach (no admin) NO ve la opción 'Todas las divisiones' en el DivisionSelector"
    - "Empty state se muestra cuando no hay partidos en la división"
  artifacts:
    - path: "src/context/DivisionContext.tsx"
      provides: "Sentinel ALL_DIVISIONS + userRole en el contexto"
      contains: "ALL_DIVISIONS_SENTINEL"
    - path: "src/components/layout/DivisionSelector.tsx"
      provides: "Opción 'Todas las divisiones' visible solo para admin"
      contains: "Todas las divisiones"
    - path: "src/app/(app)/fixture/page.tsx"
      provides: "Server Component que orquesta lista o agrupado-por-división"
      contains: "listByDivision"
    - path: "src/components/fixture/MatchCard.tsx"
      provides: "Card de un partido enlazado a /fixture/[id]"
      contains: "Link href={`/fixture/${"
    - path: "src/components/fixture/ResultBadge.tsx"
      provides: "Badge ganado/perdido/pendiente con score inline"
      contains: "Ganado"
  key_links:
    - from: "src/app/(app)/fixture/page.tsx"
      to: "src/lib/queries/matches.ts"
      via: "import listByDivision, listAllJuvenile"
      pattern: "from '@/lib/queries/matches'"
    - from: "src/components/layout/DivisionSelector.tsx"
      to: "DivisionContext.userRole"
      via: "useDivision().userRole"
      pattern: "userRole === 'admin'"
    - from: "src/components/fixture/MatchCard.tsx"
      to: "/fixture/[matchId]"
      via: "Link"
      pattern: "/fixture/\\$\\{"
---

<objective>
Reemplazar el PlaceholderScreen de `/fixture` por la vista real del fixture: lista de cards de partidos ordenados por fecha, con badge de resultado y chip de subequipo. Extender `DivisionContext` con userRole y un sentinel `ALL_DIVISIONS` para que el admin pueda seleccionar "Todas las divisiones" y ver el fixture agrupado por división.

Purpose: Entrega FIX-04 (coach ve fixture) y ADM-02 (admin ve todas las divisiones agrupadas). Es el cimiento visual del que el Plan 03 (form), Plan 04 (detalle) y Plan 05 (import) van a colgar el FAB, la navegación y el link.

Output: Tab "Fixture" del bottom nav funcional, cards con datos reales, navegación preparada hacia `/fixture/[id]` (la pantalla destino la entrega Plan 04 — los links existen ya).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-fixture-management/02-CONTEXT.md
@.planning/phases/02-fixture-management/02-RESEARCH.md
@.planning/phases/02-fixture-management/02-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Interfaces que el executor consume y debe respetar -->

From src/lib/queries/matches.ts (Plan 01):
```typescript
export async function listByDivision(divisionId: string): Promise<MatchRow[]>
export async function listAllJuvenile(): Promise<MatchWithDivision[]>
export type MatchWithDivision = MatchRow & { divisions: { name: string } | null }
```

From src/lib/matches/utils.ts (Plan 01):
```typescript
export function getMatchResult(scoreHome: number | null, scoreAway: number | null, matchDate: string): 'won' | 'lost' | 'draw' | 'pending'
export function formatMatchDate(isoDate: string): string  // 'Sáb 11/04'
```

From src/lib/supabase/database.types.ts (Plan 01):
```typescript
type MatchRow = Database['public']['Tables']['matches']['Row']
// Fields: id, division_id, match_date, match_time, fecha_nro, rival, home_away ('local'|'visitante'), venue, subequipo, score_home, score_away, manual, created_by, created_at, updated_at
```

From src/components/layout/AppHeader.tsx (existing — must continue working):
Renderiza DivisionSelector — no se cambia su contrato externo.

From src/app/(app)/layout.tsx (existing):
- Ya carga `profile.role` y `juvenileDivisions`.
- Pasa `userRole` al `BottomNav`.
- Llama `<DivisionProvider initialDivisions={juvenileDivisions}>` — hay que extender la prop.

From src/components/ui/card.tsx, badge.tsx (shadcn instalados):
- `<Card><CardContent>` con clases `p-4` y bordes ya definidos
- `<Badge variant="secondary">` disponible
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extender DivisionContext con userRole + sentinel ALL_DIVISIONS y propagar desde AppLayout</name>
  <files>src/context/DivisionContext.tsx, src/app/(app)/layout.tsx</files>
  <read_first>
    - src/context/DivisionContext.tsx (estado actual completo)
    - src/app/(app)/layout.tsx (línea ~76 donde define `userRole` y línea ~79 donde monta `<DivisionProvider>`)
    - src/hooks/useDivision.ts (si existe — verificar export pattern; DivisionSelector lo importa de '@/hooks/useDivision')
    - .planning/phases/02-fixture-management/02-RESEARCH.md (§ Pattern 4 — DivisionContext extendido)
  </read_first>
  <action>
**Paso 1 — actualizar `src/context/DivisionContext.tsx` con userRole y el sentinel ALL_DIVISIONS:**

```typescript
'use client'

import { createContext, useContext, useState } from 'react'

const STORAGE_KEY = 'vrc_active_division'

export const ALL_DIVISIONS_ID = '__all__'

export type Division = { id: string; name: string }

export const ALL_DIVISIONS_SENTINEL: Division = {
  id: ALL_DIVISIONS_ID,
  name: 'Todas las divisiones',
}

export function isAllDivisions(d: Division | null): boolean {
  return d?.id === ALL_DIVISIONS_ID
}

export type UserRole = 'admin' | 'coach' | 'tutora'

type DivisionContextType = {
  activeDivision: Division | null
  setActiveDivision: (d: Division) => void
  divisions: Division[]
  userRole: UserRole
}

const DivisionContext = createContext<DivisionContextType | null>(null)

export function DivisionProvider({
  children,
  initialDivisions,
  userRole,
}: {
  children: React.ReactNode
  initialDivisions: Division[]
  userRole: UserRole
}) {
  const [activeDivision, setActiveDivisionState] = useState<Division | null>(
    () => {
      if (typeof window === 'undefined') return initialDivisions[0] ?? null
      const stored = localStorage.getItem(STORAGE_KEY)
      if (stored) {
        try {
          const parsed = JSON.parse(stored) as Division
          // Allow restoring the ALL_DIVISIONS sentinel only for admin
          if (parsed.id === ALL_DIVISIONS_ID) {
            return userRole === 'admin' ? ALL_DIVISIONS_SENTINEL : (initialDivisions[0] ?? null)
          }
          const valid = initialDivisions.find((d) => d.id === parsed.id)
          return valid ?? initialDivisions[0] ?? null
        } catch {
          return initialDivisions[0] ?? null
        }
      }
      return initialDivisions[0] ?? null
    }
  )

  function setActiveDivision(d: Division) {
    setActiveDivisionState(d)
    localStorage.setItem(STORAGE_KEY, JSON.stringify(d))
  }

  return (
    <DivisionContext.Provider
      value={{ activeDivision, setActiveDivision, divisions: initialDivisions, userRole }}
    >
      {children}
    </DivisionContext.Provider>
  )
}

export function useDivision(): DivisionContextType {
  const ctx = useContext(DivisionContext)
  if (!ctx) throw new Error('useDivision must be used within DivisionProvider')
  return ctx
}
```

**Paso 2 — actualizar `src/app/(app)/layout.tsx`:** localizar la línea `<DivisionProvider initialDivisions={juvenileDivisions}>` y reemplazarla por:

```tsx
<DivisionProvider initialDivisions={juvenileDivisions} userRole={userRole as 'admin' | 'coach' | 'tutora'}>
```

(la variable `userRole` ya existe en el scope del layout, en la línea ~76).

**Paso 3 — verificar `src/hooks/useDivision.ts`** si existe — debe re-exportar el `useDivision` del contexto. Si el contenido es simplemente `export { useDivision } from '@/context/DivisionContext'`, no se necesita cambio. Si hay otra implementación, actualizarla para que coincida con el nuevo tipo de retorno (incluye `userRole`).

Backward compat: ningún componente de Fase 1 usa `userRole` desde el contexto, así que es additive-only. Los componentes que sí usan `activeDivision?.id` deben seguir funcionando — el sentinel `__all__` no aparece en `divisions[]` (la lista mantiene solo divisiones reales), y solo se inserta como opción en el `DivisionSelector` (siguiente task).
  </action>
  <verify>
    <automated>grep -c "ALL_DIVISIONS_SENTINEL" src/context/DivisionContext.tsx && grep -c "userRole" src/context/DivisionContext.tsx && grep -c "userRole={userRole" "src/app/(app)/layout.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const ALL_DIVISIONS_ID = '__all__'" src/context/DivisionContext.tsx` returns 1
    - `grep -c "ALL_DIVISIONS_SENTINEL" src/context/DivisionContext.tsx` returns at least 2
    - `grep -c "export function isAllDivisions" src/context/DivisionContext.tsx` returns 1
    - `grep -c "userRole: UserRole" src/context/DivisionContext.tsx` returns at least 1
    - `grep -c "userRole={userRole" "src/app/(app)/layout.tsx"` returns 1
    - `npx tsc --noEmit` runs without new TypeScript errors
  </acceptance_criteria>
  <done>
    DivisionContext exporta `ALL_DIVISIONS_SENTINEL`, `ALL_DIVISIONS_ID`, `isAllDivisions`, `useDivision()` retorna `userRole`. AppLayout pasa `userRole` al provider. Componentes existentes siguen funcionando.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extender DivisionSelector con opción 'Todas las divisiones' (admin-only)</name>
  <files>src/components/layout/DivisionSelector.tsx</files>
  <read_first>
    - src/components/layout/DivisionSelector.tsx (estado actual completo)
    - src/context/DivisionContext.tsx (recién actualizado — para usar ALL_DIVISIONS_SENTINEL, ALL_DIVISIONS_ID, isAllDivisions y userRole)
    - .planning/phases/02-fixture-management/02-UI-SPEC.md (§ Component Inventory — DivisionSelector behavior)
    - .planning/phases/02-fixture-management/02-RESEARCH.md (§ Pitfall 6 — opción admin-only)
  </read_first>
  <action>
Reemplazar `src/components/layout/DivisionSelector.tsx` por:

```typescript
'use client'

import { useDivision, ALL_DIVISIONS_SENTINEL, ALL_DIVISIONS_ID } from '@/context/DivisionContext'
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from '@/components/ui/popover'
import { Button } from '@/components/ui/button'
import { ChevronDown } from 'lucide-react'

export function DivisionSelector() {
  const { activeDivision, setActiveDivision, divisions, userRole } = useDivision()

  // For admin we always show the selector (incluso con 1 división) because they may want "Todas".
  // For non-admin: original behavior (hide if < 2 divisions).
  if (userRole !== 'admin' && divisions.length < 2) return null

  const options = userRole === 'admin'
    ? [...divisions, ALL_DIVISIONS_SENTINEL]
    : divisions

  return (
    <Popover>
      <PopoverTrigger asChild>
        <Button
          variant="secondary"
          size="sm"
          className="h-9 rounded-full px-3 gap-1 text-sm font-medium border border-[color:var(--primary)]"
        >
          {activeDivision?.name ?? 'División'}
          <ChevronDown size={14} />
        </Button>
      </PopoverTrigger>
      <PopoverContent align="end" className="w-48 p-1">
        {options.map((d) => {
          const isActive = activeDivision?.id === d.id
          const isAll = d.id === ALL_DIVISIONS_ID
          return (
            <button
              key={d.id}
              onClick={() => setActiveDivision(d)}
              className={[
                'w-full text-left px-3 py-2 rounded text-sm transition-colors',
                isActive
                  ? 'bg-primary/20 text-primary font-semibold border border-[color:var(--primary)]'
                  : 'hover:bg-secondary text-foreground',
                isAll && !isActive ? 'border-t border-border mt-1 pt-2' : '',
              ].join(' ')}
            >
              {d.name}
            </button>
          )
        })}
      </PopoverContent>
    </Popover>
  )
}
```

Notas:
- "Todas las divisiones" se agrega al final de la lista visible.
- Si el usuario NO es admin, la opción ni siquiera entra al array `options` — defensa en profundidad además de la RLS.
- El borde superior + margen visualmente separa "Todas" de las divisiones específicas (UI-SPEC).
  </action>
  <verify>
    <automated>grep -c "ALL_DIVISIONS_SENTINEL" src/components/layout/DivisionSelector.tsx && grep -c "userRole === 'admin'" src/components/layout/DivisionSelector.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { useDivision, ALL_DIVISIONS_SENTINEL, ALL_DIVISIONS_ID } from '@/context/DivisionContext'" src/components/layout/DivisionSelector.tsx` returns 1
    - `grep -c "userRole === 'admin'" src/components/layout/DivisionSelector.tsx` returns at least 1
    - `grep -c "...divisions, ALL_DIVISIONS_SENTINEL" src/components/layout/DivisionSelector.tsx` returns 1
    - `npx tsc --noEmit` runs without new TypeScript errors
    - El componente sigue siendo un Client Component (`'use client'` en la primera línea)
  </acceptance_criteria>
  <done>
    Admin ve "Todas las divisiones" en el selector. Coach no. Selección de "Todas" actualiza activeDivision al sentinel.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Crear ResultBadge, MatchCard, FixtureList, EmptyFixture y la página /fixture con loading state</name>
  <files>
    src/components/fixture/ResultBadge.tsx,
    src/components/fixture/MatchCard.tsx,
    src/components/fixture/FixtureList.tsx,
    src/components/fixture/EmptyFixture.tsx,
    src/app/(app)/fixture/page.tsx,
    src/app/(app)/fixture/loading.tsx
  </files>
  <read_first>
    - src/app/(app)/fixture/page.tsx (estado actual — actualmente renderiza PlaceholderScreen)
    - src/components/ui/card.tsx (verificar exports Card, CardContent)
    - src/components/ui/badge.tsx (verificar exports Badge y variantes disponibles)
    - src/components/ui/skeleton.tsx (verificar export Skeleton)
    - .planning/phases/02-fixture-management/02-UI-SPEC.md (§ Component Inventory — MatchCard structure, ResultBadge, Empty states, Loading states)
    - .planning/phases/02-fixture-management/02-RESEARCH.md (§ Code Examples — MatchCard, § Architecture Patterns — Pattern 1)
    - src/lib/queries/matches.ts (Plan 01 — listByDivision, listAllJuvenile, MatchWithDivision)
    - src/lib/matches/utils.ts (Plan 01 — getMatchResult, formatMatchDate)
    - src/context/DivisionContext.tsx (para useDivision + ALL_DIVISIONS_ID + isAllDivisions)
  </read_first>
  <action>
**Paso 1 — `src/components/fixture/ResultBadge.tsx`:**

```typescript
import { Badge } from '@/components/ui/badge'
import type { MatchResult } from '@/lib/matches/utils'

type ResultBadgeProps = {
  result: MatchResult
  scoreHome: number | null
  scoreAway: number | null
}

export function ResultBadge({ result, scoreHome, scoreAway }: ResultBadgeProps) {
  const hasScore = scoreHome !== null && scoreAway !== null
  const scoreText = hasScore ? ` ${scoreHome}-${scoreAway}` : ''

  if (result === 'won') {
    return (
      <Badge
        className="bg-green-700 text-green-100 hover:bg-green-700"
        aria-label={`Resultado: Ganado${scoreText}`}
      >
        Ganado{scoreText}
      </Badge>
    )
  }
  if (result === 'lost') {
    return (
      <Badge
        className="bg-red-700 text-red-100 hover:bg-red-700"
        aria-label={`Resultado: Perdido${scoreText}`}
      >
        Perdido{scoreText}
      </Badge>
    )
  }
  if (result === 'draw') {
    return (
      <Badge variant="secondary" aria-label={`Resultado: Empate${scoreText}`}>
        Empate{scoreText}
      </Badge>
    )
  }
  return (
    <Badge variant="secondary" aria-label="Resultado: Pendiente">
      Pendiente
    </Badge>
  )
}
```

**Paso 2 — `src/components/fixture/MatchCard.tsx`:**

```typescript
import Link from 'next/link'
import { ChevronRight } from 'lucide-react'
import { Card, CardContent } from '@/components/ui/card'
import { ResultBadge } from './ResultBadge'
import { formatMatchDate, getMatchResult } from '@/lib/matches/utils'

type MatchCardProps = {
  id: string
  match_date: string
  rival: string
  home_away: 'local' | 'visitante'
  match_time: string | null
  fecha_nro: number | null
  subequipo: string | null
  score_home: number | null
  score_away: number | null
}

export function MatchCard({
  id,
  match_date,
  rival,
  home_away,
  match_time,
  fecha_nro,
  subequipo,
  score_home,
  score_away,
}: MatchCardProps) {
  const result = getMatchResult(score_home, score_away, match_date)
  const isPastUnscored = result === 'pending' && new Date(match_date + 'T23:59:59') < new Date()
  const timeShort = match_time ? match_time.slice(0, 5) : null

  return (
    <Link
      href={`/fixture/${id}`}
      className="block rounded-lg focus-visible:ring-2 focus-visible:ring-[color:var(--ring)]"
    >
      <Card className="bg-card border-border hover:bg-accent transition-colors min-h-[72px]">
        <CardContent className="p-4 flex flex-col gap-1">
          <div className="flex items-center justify-between gap-2">
            <span className="text-sm text-muted-foreground">{formatMatchDate(match_date)}</span>
            <div className="flex items-center gap-1.5">
              {fecha_nro != null && (
                <span className="text-sm text-muted-foreground">Fecha #{fecha_nro}</span>
              )}
              {subequipo && (
                <span className="bg-secondary text-secondary-foreground text-xs font-semibold px-1.5 py-0.5 rounded">
                  {subequipo}
                </span>
              )}
              <ResultBadge result={result} scoreHome={score_home} scoreAway={score_away} />
            </div>
          </div>
          <p className="text-base font-semibold leading-tight truncate">{rival}</p>
          <div className="flex items-center justify-between text-sm text-muted-foreground">
            <span>
              {home_away === 'local' ? 'Local' : 'Visitante'}
              {timeShort ? ` · ${timeShort}` : ''}
              {isPastUnscored ? ' · Sin resultado cargado' : ''}
            </span>
            <ChevronRight className="h-5 w-5 text-muted-foreground shrink-0" />
          </div>
        </CardContent>
      </Card>
    </Link>
  )
}
```

**Paso 3 — `src/components/fixture/EmptyFixture.tsx`:**

```typescript
import Link from 'next/link'
import { CalendarDays } from 'lucide-react'

export function EmptyFixture({ isAdmin }: { isAdmin: boolean }) {
  return (
    <div className="flex flex-col items-center justify-center py-16 px-4 text-center">
      <CalendarDays className="h-12 w-12 text-muted-foreground mb-4" aria-hidden />
      <h2 className="text-xl font-semibold mb-2">
        {isAdmin ? 'Sin partidos cargados' : 'Sin partidos programados'}
      </h2>
      <p className="text-sm text-muted-foreground mb-6 max-w-xs">
        {isAdmin
          ? 'Importá el fixture URBA o agregá un partido manualmente.'
          : 'El fixture de esta división aún no fue cargado.'}
      </p>
      {isAdmin && (
        <Link
          href="/admin/fixture-import"
          className="text-sm text-primary underline-offset-4 hover:underline"
        >
          Importar fixture
        </Link>
      )}
    </div>
  )
}
```

**Paso 4 — `src/components/fixture/FixtureList.tsx`** (Client Component — recibe los matches del Server Component y renderiza la lista o el agrupado por división):

```typescript
'use client'

import { MatchCard } from './MatchCard'
import { EmptyFixture } from './EmptyFixture'
import { useDivision, isAllDivisions } from '@/context/DivisionContext'

type MatchItem = {
  id: string
  division_id: string
  division_name?: string
  match_date: string
  rival: string
  home_away: 'local' | 'visitante'
  match_time: string | null
  fecha_nro: number | null
  subequipo: string | null
  score_home: number | null
  score_away: number | null
}

type FixtureListProps = {
  matches: MatchItem[]
}

export function FixtureList({ matches }: FixtureListProps) {
  const { activeDivision, userRole } = useDivision()
  const showGrouped = isAllDivisions(activeDivision)

  if (matches.length === 0) {
    return <EmptyFixture isAdmin={userRole === 'admin'} />
  }

  if (!showGrouped) {
    return (
      <div className="px-4 pt-4 pb-24 space-y-3">
        {matches.map((m) => (
          <MatchCard key={m.id} {...m} />
        ))}
      </div>
    )
  }

  // Grouped by division for admin "Todas las divisiones"
  const groups = new Map<string, MatchItem[]>()
  for (const m of matches) {
    const key = m.division_name ?? m.division_id
    if (!groups.has(key)) groups.set(key, [])
    groups.get(key)!.push(m)
  }
  const sortedKeys = Array.from(groups.keys()).sort()

  return (
    <div className="pt-2 pb-24">
      {sortedKeys.map((divName) => (
        <section key={divName}>
          <h2 className="text-xl font-semibold px-4 pt-6 pb-2">{divName}</h2>
          <div className="px-4 space-y-3">
            {groups.get(divName)!.map((m) => (
              <MatchCard key={m.id} {...m} />
            ))}
          </div>
        </section>
      ))}
    </div>
  )
}
```

**Paso 5 — `src/app/(app)/fixture/loading.tsx`:**

```typescript
import { Skeleton } from '@/components/ui/skeleton'

export default function FixtureLoading() {
  return (
    <div className="px-4 pt-4 space-y-3">
      {[1, 2, 3].map((i) => (
        <Skeleton key={i} className="h-[72px] rounded-lg" />
      ))}
    </div>
  )
}
```

**Paso 6 — reescribir `src/app/(app)/fixture/page.tsx`** (Server Component). Decide entre listByDivision o listAllJuvenile según el cliente seleccionó "Todas". Como `activeDivision` vive en el contexto (client), el patrón es:

```typescript
import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
import { listAllJuvenile } from '@/lib/queries/matches'
import { FixtureListShell } from '@/components/fixture/FixtureListShell'

export default async function FixturePage() {
  const supabase = createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const { data: profileData } = await (supabase as any)
    .from('profiles').select('role').eq('id', user.id).single()
  const role = (profileData?.role ?? 'coach') as 'admin' | 'coach' | 'tutora'

  // Always fetch all juvenile matches when admin (cheap query, small fixture);
  // the client filters by activeDivision. For coaches, RLS already restricts to their divisions.
  const matches = await listAllJuvenile()

  const items = matches.map((m) => ({
    id: m.id,
    division_id: m.division_id,
    division_name: m.divisions?.name ?? '',
    match_date: m.match_date,
    rival: m.rival,
    home_away: m.home_away as 'local' | 'visitante',
    match_time: m.match_time,
    fecha_nro: m.fecha_nro,
    subequipo: m.subequipo,
    score_home: m.score_home,
    score_away: m.score_away,
  }))

  return <FixtureListShell allMatches={items} isAdmin={role === 'admin'} />
}
```

Y crear el shell client component `src/components/fixture/FixtureListShell.tsx`:

```typescript
'use client'

import { useMemo } from 'react'
import { useDivision, isAllDivisions } from '@/context/DivisionContext'
import { FixtureList } from './FixtureList'

type MatchItem = Parameters<typeof FixtureList>[0]['matches'][number]

export function FixtureListShell({ allMatches, isAdmin }: { allMatches: MatchItem[]; isAdmin: boolean }) {
  const { activeDivision } = useDivision()
  const filtered = useMemo(() => {
    if (!activeDivision || isAllDivisions(activeDivision)) {
      return isAdmin ? allMatches : []
    }
    return allMatches.filter((m) => m.division_id === activeDivision.id)
  }, [activeDivision, allMatches, isAdmin])

  return <FixtureList matches={filtered} />
}
```

NOTA importante sobre RLS: `listAllJuvenile()` corre server-side; RLS filtra automáticamente a las divisiones del coach (las policy `matches_read` exige role=admin o coach_divisions match). Para admin trae todo; para coach trae solo lo suyo. El filtro en cliente es solo para la UX (cambio entre divisiones sin re-fetch). Esto es seguro.

Para coaches con 1 sola división, la lista filtrada será la suya y el shell funciona sin cambios.

**Paso 7 — agregar `src/components/fixture/FixtureListShell.tsx`** al array de `files_modified` del summary (ya lo tenemos en la lista).

Después de los cambios, ejecutar `npx tsc --noEmit` y arreglar cualquier error de tipo (en particular el cast del `home_away` y los nullables).
  </action>
  <verify>
    <automated>grep -c "MatchCard" src/components/fixture/MatchCard.tsx && grep -c "listAllJuvenile" "src/app/(app)/fixture/page.tsx" && grep -c "ResultBadge" src/components/fixture/ResultBadge.tsx && grep -c "Skeleton" "src/app/(app)/fixture/loading.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/fixture/ResultBadge.tsx` exports `ResultBadge` and contains 'Ganado', 'Perdido', 'Empate', 'Pendiente' (`grep -c "Ganado\|Perdido\|Empate\|Pendiente" src/components/fixture/ResultBadge.tsx` returns at least 4)
    - File `src/components/fixture/MatchCard.tsx` contains `Link href={`/fixture/${id}`}` (verbatim grep on `/fixture/\$\{`)
    - File `src/components/fixture/MatchCard.tsx` uses `getMatchResult` and `formatMatchDate` (grep both)
    - File `src/components/fixture/MatchCard.tsx` contains 'Sin resultado cargado' string for past-unscored matches
    - File `src/components/fixture/FixtureList.tsx` is a Client Component (`'use client'` first non-comment line)
    - File `src/components/fixture/FixtureList.tsx` references `isAllDivisions(activeDivision)` for grouped render
    - File `src/components/fixture/EmptyFixture.tsx` contains 'Sin partidos programados' AND 'Sin partidos cargados'
    - File `src/app/(app)/fixture/page.tsx` is a Server Component (no `'use client'`) and calls `listAllJuvenile()`
    - File `src/app/(app)/fixture/page.tsx` does NOT import `PlaceholderScreen` anymore (`grep -c "PlaceholderScreen" "src/app/(app)/fixture/page.tsx"` returns 0)
    - File `src/app/(app)/fixture/loading.tsx` renders 3 `Skeleton` with `h-[72px]` class
    - `npx tsc --noEmit` runs without new errors
    - `npm run build` succeeds (Next.js build does not error on the fixture route)
  </acceptance_criteria>
  <done>
    `/fixture` muestra los partidos reales (no PlaceholderScreen). Coach con 1 división ve sus partidos. Admin con "Todas las divisiones" ve agrupado por división. Empty state aparece con el copy correcto. Loading state con skeletons. Cards navegan a `/fixture/[id]` (la pantalla destino la entrega Plan 04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DivisionContext | Selección de "Todas las divisiones" no debe exponer divisiones de otros clubes a un coach |
| Server Component → Supabase | RLS es la fuente de verdad de qué partidos puede ver cada user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Elevation of Privilege | DivisionSelector | mitigate | La opción "Todas las divisiones" no se agrega al array de opciones si `userRole !== 'admin'`; defensa además de la RLS |
| T-02-09 | Information Disclosure | FixturePage | mitigate | `listAllJuvenile()` corre server-side; RLS filtra a las divisiones del coach automáticamente. El filtro client-side es solo UX. |
| T-02-10 | Tampering | DivisionContext localStorage | mitigate | Al restaurar el sentinel `__all__` desde localStorage, se verifica `userRole === 'admin'`; si no, fallback a la primera división real |
| T-02-11 | Spoofing | activeDivision sentinel | accept | Un coach que manipule el localStorage para forzar sentinel `__all__` igualmente solo verá lo que la RLS permite (sus divisiones) — riesgo residual mínimo |
</threat_model>

<verification>
- `/fixture` carga sin errores y NO renderiza `PlaceholderScreen`
- Coach con M19 asignada ve solo partidos M19
- Admin con "Todas las divisiones" ve secciones M15/M16/M17/M19
- Coach NO ve "Todas las divisiones" en el dropdown
- Empty state muestra copy correcto según rol
- `npm run build` exitoso
</verification>

<success_criteria>
- FIX-04 implementado: coach ve fixture ordenado por fecha en su división
- ADM-02 implementado: admin ve todas las divisiones agrupadas
- Cards navegan a `/fixture/[matchId]` (destino lo crea Plan 04)
- DivisionContext expone `userRole`, `ALL_DIVISIONS_SENTINEL`, `isAllDivisions()`
</success_criteria>

<output>
After completion, create `.planning/phases/02-fixture-management/02-02-SUMMARY.md`.
</output>
</content>
</invoke>